# PROGRESS.md — 工地危险样本扩充项目（当前口径 v2，2026-09-18 起）

> ## 约定
> - 需求 prompt 写入 `D:\yolo\Prompt.md`（仅用户可编辑，AI 只读）。
> - 本文件是**唯一进度台账**：每轮工作完成并验证后立即更新（结果数字、产物路径、下一步、新坑）；改前先读当前版本，改完刷新文首「截止」行。
> - 命令/路径变化必须同步 `COMMANDS.md`；全文禁止引用不存在的文件。
> - 弃用文件一律移入 `_trash\`（勿真删）；已舍弃的历史不再写进本文件。

> 截止（2026-09-18 晚）：**发现重大数据泄漏问题并已隔离**——全部 1085 张生成图都是真实照片的 **Qwen-Image-Edit 编辑版**（93 张真实照片全部被用作编辑源），其中 **611 张（56%）源于 v2 test33** → 生成臂训练池污染，**gen 系列结果全部作废重做**（real60 / real93 真实基线不受影响）；无泄漏池已另存 `D:\gas_cylinders\gen_clean\`（**474 张**，原图保留未删）。同日此前完成（部分数字已作废）：v2 口径落地（train60/test33 + last.pt 协议）、真实基线 real60 六权重、训练预算对照（其真实臂结论仍有效）。**重做路线待定**：A = 只用 gen_clean 474 张重训；B =（推荐）另收一批「从未被用作编辑源」的真实照片做新 test，则 1085 张可全部继续用、实验规模更大。待做实验清单见 `List_of_Experiments.md`。

## 一、项目与目标

- **一句话**：工地文明程度提高 → 危险样本（违规场景照片）自然稀缺，而故意摆拍有安全与伦理问题 → 用**本地大模型生成式扩充**稀缺危险样本（真实性红线）→ 做多目标检测。第一步 = 气瓶「放置问题」，第二步 = 不带安全帽的工人。
- **检测目标**：Placement Issues——气瓶**未放置在专用固定架或手推车上**即为目标（监理口径：统一归入「放置问题·未固定」，含倒地）。
- **正/负样本**：正 = 自由放置的气瓶；负 = 固定架上的闲置气瓶 / 手推车上的使用中气瓶。
- **生成质量红线**：必须带喷漆磨损、污渍、锈迹（真实照片里没有干净气瓶；防「过净」域差导致真实场景漏检）。
- **生图工具链（2026-09-18 更正）**：**实际在用 = Qwen-Image-Edit-2509 图生图编辑**——全部 1085 张生成图的工作流（PNG `prompt` 元数据）都是 `LoadImage(真实照片) → TextEncodeQwenImageEditPlus → KSampler`，**没有一张是文生图**（旧文档写「z-image-turbo 文生图在用」有误）。canny ControlNet 仍为试验项。

## 二、数据现状（`D:\gas_cylinders\`，工作区外）

### 2.1 真实照片 93 张（人工标注完成）
- `real_photo\93_real_photos\`：57 张有框共 179 框 + 36 张负样本（空标）；单类 `0: Placement Issues`。
- 分层划分（seed=42）：train 75（46 正 + 29 负）/ val 18（11 正 + 7 负，30 框），清单在 `v1_split\`（另含 all.txt 93 行全量清单）。
- **红线：93 张绝不参与生成式数据训练与选型**——既是生成数据的全量 test，也是干净子集（v1 = val18）的来源。
- **v2 划分（2026-09-18 已执行）**：93 → **train 60（37 正 + 23 负，113 框）/ test 33（20 正 + 13 负，66 框）**（按正/负分层、seed=42；脚本 `scripts\splits_real_v2\make_split_real_v2.py`，输出 `v2_split\`）。**test33 取代 val18（18 图/30 框）成为唯一干净口径**——只评测，任何模型不训练、不选权；全篇选权协议统一为固定 100 轮报 last.pt。v1_split 保留（v1 结果归档于 `phase6_gen493_summary.json`）。

### 2.2 生成图 493 张（第一批，gen493）
- `Placement_Issues\`：**493 张**= **380 张正样本（1062 框）+ 113 张负样本（空标）**；单类同上；分辨率约 1184×880。
- 生产流程：生图 → 打标模型预标注 → annotator GUI **逐张人工复核**（500 张经复核删 7 张）。
- 分层划分（seed=42，`gen493_split\`）：train 394（304 正/875 框 + 90 负）/ val 99（76 正/187 框 + 23 负）；**test = 93 张真实照片全量**。

### 2.3 生成图 592 张（第二批，2026-09-17 复核完成，待划分）
- `Placement_Issues_2\`：**592 张**（编号 0501–1110，缺 18 号 = 生成剔除）= **414 张正样本（1129 框）+ 178 张负样本（空标）**；分辨率以 1184×880 为主。
- 时间线：2026-09-14 → 09-16 生成 → 打标一键工具预打标（mix586_all 模型）→ annotator GUI 逐张复核，09-17 15:36 收尾（其中 289 张在复核中被修改）。
- **尚未划分、尚未参与任何训练**；与第一批合并（1085 张）重训为下一步（见 `List_of_Experiments.md`）。
- `Placement_Issues_2\data.yaml`（`path: .` / `train: images`）仅供 annotator GUI，**勿用于训练**。

### 2.4 生成图来源审计与无泄漏池 gen_clean（2026-09-18，重要）
- **发现**：全部 1085 张生成图都是「真实照片的编辑版」，编辑源（ComfyUI `LoadImage`）覆盖 **93/93 张真实照片**（test33 33/33、train60 60/60 全被用作源）；每源编辑次数不均（min 5 / median 14 / max 139）。
- **后果**：源图 ∈ test33 的生成样本会把测试内容带进训练 → **611 张（56%）必须从训练/验证池剔除**；其中第一批 493 张里有 350 张受影响（b1 仅剩 143），第二批 592 里剩 331。
- **无泄漏池**：`D:\gas_cylinders\gen_clean\`（**474 图 + 474 标签**，712 MB，原两批原样保留不动）；`manifest.json` 记录逐图来源与 kept/dropped 明细；审计记录 `D:\gas_cylinders\gen_sources.json`。
- **生成源白名单**：`D:\gas_cylinders\gen_source_pool\`（**train60 的 60 张副本**，images+labels+manifest）——**后续用 Qwen-Image-Edit 继续生成时只从这里取图**，test33 的 33 张永不作为编辑源（manifest 里同时列出禁用名单）。注意：若改走「新 test」路线，本池需重建。
- **影响面**：test93 对生成臂**不再干净**（93 张里 60 张是训练图的编辑源）→ 以后只能报 test33（干净）；v1/v2 的全部 gen 数字（gen493 系列、gen1085）作废重做；real60/real93 不受影响。

### 2.5 其它素材
- `Hardhat\`：2 张真实图，第二目标「不带安全帽的工人」早期素材（本文件旧版写作「安全帽」，以实际目录名 `Hardhat` 为准）。

## 三、结果与结论（v2 口径，2026-09-18）

### 三-A、无泄漏口径初步结果（2026-09-18 晚，`phase9_genclean_yolo26s.json`）

**设置**：gen_clean 池（474 张，全部源图 ∉ test33）按分层 80/20 → **train 379 / val 95**；yolo26s 从 COCO 预训练、100 轮 `patience=0`、报 last.pt；干净口径 test33。

| 训练数据 | 规模 | test33 P / R | **mAP50 / mAP50-95** |
|---|---|---|---|
| real60（原始真实照片） | 60 | 0.598 / 0.496 | 0.479 / 0.268 |
| real60 加练 300 轮 | 60 | — | 0.520 / 0.286 |
| **gen_clean（无泄漏生成）** | **379** | **0.676 / 0.621** | **0.611 / 0.377** |
| gen493（污染，仅对照） | 394 | — | 0.778 / 0.548 |
| gen1085（污染，仅对照） | 868 | — | 0.842 / 0.674 |

- **初步结论**：无泄漏口径下「编辑合成数据 > 真实数据」仍然成立（mAP50 +0.132、mAP50-95 +0.109，相对 +28% / +41%），但**优势幅度比污染口径小得多**——此前 0.842/0.674 的漂亮数字里，很大一部分来自 test 内容泄漏。
- 注意：单权重（yolo26s）、单种子、test33 仅 66 框 → 结论噪声大，需多权重/多种子复现后才可写进论文。
- gen_clean 池只源于 60 张源照片 → 这个增益的本质是「60 张照片的状态编辑」，论文口径要照此改。

### 三-B、已作废的污染口径数字（仅存档）

> ⚠️ **本节全部生成臂（gen493 / gen1085）数字已作废（2026-09-18 傍晚）**：审计发现生成图都是真实照片的编辑版、且 test33 的 33 张全部被用作编辑源（见 2.4）——训练池含测试内容，test93/test33 口径对生成臂均不干净。**有效数据**：real60 / real93 基线（不受影响）+ 训练预算对照里真实臂的部分。重做方案待定（可选项：A 只用 gen_clean 474 张重训；B 另收一批「从未被用作编辑源」的真实照片做新 test，则全部 1085 张可继续用且实验规模更大）。

**设置**：六权重（YOLOv8 / YOLOv11 / YOLO26 × s/m）统一超参（epochs=100 / imgsz=640 / batch=16 / device=0），**统一取 last.pt（固定轮数，不做验证集选权）**；生成图 train/val = 394/99；真实基线 = **v2 train60（60 张）**；**干净口径 = v2 test33（33 图 / 66 框，任何模型不训练、不选权）**；test93 仅对生成数据模型干净。汇总数字：`phase7_realv2_summary.json`。

**主结果**（mAP50 / mAP50-95）：

| 权重 | gen493 test93（干净） | gen493 test33（干净） | real60 test33（干净） | real60 test93（污染·虚高） | 差值 gen−real（mAP50-95） |
|---|---|---|---|---|---|
| yolov8s | 0.725 / 0.562 | 0.743 / 0.531 | 0.454 / 0.219 | 0.738 / 0.502（Δ+0.283） | **+0.312** |
| yolov8m | 0.699 / 0.543 | 0.702 / 0.498 | 0.044 / 0.015 | 0.201 / 0.091（Δ+0.076） | **+0.482** |
| yolo11s | 0.726 / 0.551 | 0.752 / 0.528 | 0.258 / 0.102 | 0.563 / 0.305（Δ+0.203） | **+0.426** |
| yolo11m | 0.700 / 0.528 | 0.707 / 0.492 | 0.044 / 0.013 | 0.132 / 0.046（Δ+0.033） | **+0.479** |
| **yolo26s** | **0.748 / 0.575** | **0.778 / 0.548** | 0.479 / 0.268 | 0.785 / 0.554（Δ+0.286） | **+0.280** |
| yolo26m | 0.713 / 0.551 | 0.755 / 0.528 | 0.146 / 0.060 | 0.245 / 0.117（Δ+0.057） | **+0.469** |

- 「污染·虚高」列 = real60 在 test93 上的成绩，其中 60/93 张是它的训练图（记忆效应）→ 数字虚高；`Δ` = 该列 mAP50-95 减 test33（干净）mAP50-95 的差，即虚高幅度。
- **test33 才是双方都干净的对比口径**；test93 列只作泄露分析用（对照：v1 real93 在 75/93 污染时 yolo26s 为 0.857 / 0.587，污染越多虚高越大）。
- 辅助口径：gen493 用 v1 选权规则（best.pt）在 test33 上同样 6/6 全胜（差值 +0.31~+0.48），结论不依赖选权规则。

**结论**
1. 【作废，待重做】~~**生成数据增益成立（v2 口径）**：test33 上 gen493 六权重对 real60 基线 **6/6 全胜**，优势 +0.28~+0.48 mAP50-95~~——生成臂训练池含测试图编辑版，结论不可用。
2. 【作废，待重做】~~「扩量有效、数量取胜」~~——重做后若仍成立再写回；注意新事实：生成图是**同一批真实照片的编辑版**，多样性上界 = 源照片数量（60 张），这正是论文要重新定位的点。
3. **训练预算对照（2026-09-18 完成，`phase7_300ep_check.json`）**：真实臂改 300 轮（3× 预算，`runs\detect\real60_300ep\`）后——
   - **干净 test33 上几乎不动**（mAP50-95：yolov8s 0.219→0.209、yolo26s 0.268→0.286；m 系列 0.015→0.085 / 0.013→0.052 / 0.060→0.130，仍只有生成臂 0.49~0.55 的零头）；
   - 而在**污染的 test93 上大幅虚高**（yolov8s 0.502→0.613、yolo26s 0.554→0.648）：训练越久越"背下"训练集里的测试图——**这是论文 C2 泄露分析的直接证据**（污染口径的虚高随训练预算放大）。
   - 结论：**主结论不依赖训练预算，v2 的 100 轮协议保持不变**；m 系列在 100 轮未收敛的事实归因于数据量（生成臂 ~70–90 轮即平台）。
   - 坑：固定轮数协议必须 `patience=0` 关早停（yolo11m 300 轮曾在第 105 轮被误截断，见 COMMANDS 已知坑 6）。
4. **验证路径（见 `List_of_Experiments.md`）**：① 扩量——**执行中（2026-09-18）**：合并划分 `gen1085_split\`（train 868 / val 217）已建，六权重 100 轮训练中（`runs\detect\gen1085\`）→ 完成后跑 `eval_gen1085.py` 出规模曲线（real60 60 张 / gen493 394 张 / gen1085 868 张）；② 250 vs 493 减半对比；③ **经典增广三臂对照**（A 真实60 / B 增广600 / C 生成600，v2 口径，设计见 `paper\PAPER_PLAN.md` 第四节 3）。
5. **选型**：本轮最佳 = yolo26s（test93 0.748 / 0.575；test33 0.778 / 0.548）。

**打标模型（当前生产件，2026-09-18 更换）**：`runs\detect\gen1085\yolo26s\weights\last.pt`——1085 张生成图训练（COCO 预训练、100 轮）；选型依据：在 119 张候选权重均未见过的生成图上 mAP50-95 **0.932**（P 0.970 / R 0.958），优于旧件 mix586 best（0.887）与 gen493 last（0.541）。`autolabel.py` 与一键 bat 默认已切换（打标是**工具**，不受 test33 泄漏规则约束；若将来要"干净打标器"，可从 gen_clean 池重训）。

## 四、路线图

1. ✅ 真实数据打底：93 张人工标注 + 分层划分（2026-09-02）。
2. ✅ 生成管线与首轮闭环：493 张走完「生成 → 预打标 → 人工复核 → 六权重训练 → 评测」（2026-09-14）。
3. **数量取胜验证（下一步，三项可并跑；完整清单与可开始状态见 `List_of_Experiments.md`）**：① 扩量——第二批 592 张已复核就绪（493+592=1085）→ 建合并划分 → 重训；② 250 vs 493 减半对比；③ **经典增广对照**——75 张真实离线增广到 394 张，与 gen493 同量级三臂对比（设计已定 2026-09-15，见 `paper\PAPER_PLAN.md` 第四节 3）。
4. 多目标扩展：安全帽（第二目标，数据目录名 `Hardhat\`）。
5. 论文：首投 Automation in Construction（elsarticle，2026-09-13 启动）；必补实验与写作状态见 `paper\PAPER_PLAN.md`（经典增广对照、规模实验）。

## 五、资产与踩坑

- **环境**：`D:\yolo\.venv`（Py 3.13 / torch 2.8.0+cu129 / ultralytics 8.4.135）；GPU RTX 5070 Ti Laptop 11.9 GB（640/b16 无压力）；权重源 `weights\`（v8/11/26 × s/m）。
- **代码**：全部在 `scripts\`：`prepare_labeling_workspace.bat`（一键打标）、`autolabel.py`、`splits_real_v2\`（v2 真实集划分）、`run_phase7_real60.ps1`（真实基线，支持 -Only 断点续跑）、`eval_realv2.py`（v2 评测）、`eval_real60_300ep.py`（预算对照评测）、`show_results.py`（结果查看器）、`eval_gen493.py` / `run_phase3_gen493.ps1` / `splits_gen493\`（v1 存档）、`mix586\`。
- **数据区工作区**：`D:\gas_cylinders\Placement_Issues\`（第一批 493 + `gen493_split\`）、`Placement_Issues_2\`（第二批 592）、`gen1085_split\`（合并两批，train 868/val 217）、`real_photo\93_real_photos\`（+`v1_split\` 存档、`v2_split\` 当前）、`Hardhat\`。
- **run 目录**（`runs\detect\`）：`gen493\`（生成臂六权重）、`real60\`（v2 真实基线六权重，100 轮）、`real60_300ep\`（预算对照，300 轮）、`p7\`（**评测输出**：每个「口径×权重」一个子目录，内含 confusion_matrix / PR 曲线 / val_batch 预测图等 ultralytics 标准产物）、`mix586_all\`（打标模型）、`real93\`（v1 存档）。
- **沙箱**：训练/评测/autolabel 需 danger-full-access；纯数据脚本 workspace-write 可跑；跨盘写（`D:\gas_cylinders`）也要升级。
- **踩坑**（完整表见 COMMANDS.md「已知坑」）：data.yaml 的 `path` 与清单行必须绝对路径；评测脚本须 `__main__` 守卫 + `workers=0`；打标全量重训自评虚高；`.gitignore` 不支持行内注释。

## 六、文件地图

| 路径 | 内容 |
|---|---|
| `PROGRESS.md` | **本文件**：唯一进度台账 |
| `COMMANDS.md` | 命令表（AI agent 用） |
| `List_of_Experiments.md` | 待做实验一页清单（含新增建议，2026-09-17 立） |
| `Prompt.md` | 用户 prompt（私有，不入库） |
| `scripts\` | 全部代码（清单见第五节） |
| `annotator\` | 标注 GUI（`start_annotator.bat` / `stop_server.bat`） |
| `runs\detect\` | 权重与评测产物：`gen493\`（生成臂）、`real60\`（v2 真实基线）、`real60_300ep\`（预算对照）、`p7\`（评测图：混淆矩阵/PR 曲线/预测样例）、`mix586_all\`（打标模型）、`real93\`（v1 存档） |
| `phase7_realv2_summary.json` | **v2 结果汇总（权威数字）** |
| `phase7_300ep_check.json` | 训练预算对照（100 vs 300 轮，附录证据） |
| `phase6_gen493_summary.json` | v1 口径结果（存档） |
| `logs\` | 当前阶段日志（gen493 / mix586 / phase6） |
| `weights\` | 预训练源权重（v8/11/26 × s/m） |
| `paper\` | 论文工作区（reference / PAPER_PLAN / working_paper.tex / references.bib） |
| `_trash\` | **弃用文件归档区**；删前先移入，勿真删 |
| `D:\gas_cylinders\` | 数据区（仓库外），内容见第二节 |

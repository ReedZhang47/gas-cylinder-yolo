# PROGRESS.md — 工地危险样本扩充项目（v1）

> ## 约定
> - 需求 prompt 写入 `D:\yolo\Prompt.md`（仅用户可编辑，AI 只读）。
> - 本文件是**唯一进度台账**：每轮工作完成并验证后立即更新（结果数字、产物路径、下一步、新坑）；改前先读当前版本，改完刷新文首「截止」行。
> - 命令/路径变化必须同步 `COMMANDS.md`；全文禁止引用不存在的文件。
> - 弃用文件一律移入 `_trash\`（勿真删）；已舍弃的历史不再写进本文件。

> 截止（2026-09-14）：**493 张复核修订版全流程闭环**——六权重训练 69 min → 全套评测（`phase6_gen493_summary.json`）→ 打标模型 586 全量重训（`mix586_all`，autolabel/bat 默认已切换）；核心结论：**生成数据增益成立（val18 两轮 6/6 全胜）**，本轮最佳权重 yolo26s。同日：工作区整理（代码归入 `scripts\`、文档精简、弃用内容归档 `_trash\`）；论文工作区在建（见路线图 5）。

## 一、项目与目标

- **一句话**：工地文明程度提高 → 危险样本（违规场景照片）自然稀缺，而故意摆拍有安全与伦理问题 → 用**本地大模型生成式扩充**稀缺危险样本（真实性红线）→ 做多目标检测。第一步 = 气瓶「放置问题」，第二步 = 不带安全帽的工人。
- **检测目标**：Placement Issues——气瓶**未放置在专用固定架或手推车上**即为目标（监理口径：统一归入「放置问题·未固定」，含倒地）。
- **正/负样本**：正 = 自由放置的气瓶；负 = 固定架上的闲置气瓶 / 手推车上的使用中气瓶。
- **生成质量红线**：必须带喷漆磨损、污渍、锈迹（真实照片里没有干净气瓶；防「过净」域差导致真实场景漏检）。
- **生图工具链**：z-image-turbo（文生图，在用）；qwen-image-edit-2509（编辑真实照片，待接入，授权已确认）；canny ControlNet（试验项）。

## 二、数据现状（`D:\gas_cylinders\`，工作区外）

### 2.1 真实照片 93 张（人工标注完成）
- `real_photo\93_real_photos\`：57 张有框共 179 框 + 36 张负样本（空标）；单类 `0: Placement Issues`。
- 分层划分（seed=42）：train 75（46 正 + 29 负）/ val 18（11 正 + 7 负，30 框），清单在 `v1_split\`（另含 all.txt 93 行全量清单）。
- **红线：93 张绝不参与生成式数据训练与选型**——既是生成数据的全量 test，也是 val18 干净子集的来源。

### 2.2 生成图 493 张（已复核，训练主数据）
- `Placement_Issues\`：**493 张**（2026-09-14 审核删除 7 张不可用图后）= **380 张正样本（1062 框）+ 113 张负样本（空标）**；单类同上；分辨率约 1184×880。
- 生产流程：生图 → 打标模型预标注 → annotator GUI **逐张人工复核**（真实性红线的落实环节）。
- 分层划分（seed=42，`gen493_split\`）：train 394（304 正/875 框 + 90 负）/ val 99（76 正/187 框 + 23 负）；**test = 93 张真实照片全量**。

### 2.3 其它素材
- `real_photo_from_internet\`：两个 Roboflow 公开气瓶数据集（CylinDeRS 7060 张、garrafas 6750 张，CC BY 4.0，仅「气瓶」框无放置口径）——论文基线对照用。
- `安全帽\`：2 张真实图，第二目标「不带安全帽的工人」早期素材。

## 三、结果与结论（493 修订版，2026-09-14）

**设置**：六权重（YOLOv8 / YOLOv11 / YOLO26 × s/m）统一超参（epochs=100 / imgsz=640 / batch=16 / device=0）；生成图 train/val = 394/99；**test = 93 张真实照片全量**（对生成数据训练干净）；对照组 = real93 划分版六权重（75 张真实图训练）同 test 补测。汇总数字：`phase6_gen493_summary.json`（54 次 val）。

**主结果**（mAP50 / mAP50-95）：

| 权重 | test93（gen493，干净） | val18 干净口径（gen493 vs real93 基线） |
|---|---|---|
| yolov8s | 0.719 / 0.557 | 0.559 / 0.450 vs 0.416 / 0.280 |
| yolov8m | 0.703 / 0.535 | 0.558 / 0.450 vs 0.270 / 0.133 |
| yolo11s | 0.726 / 0.551 | 0.651 / 0.470 vs 0.240 / 0.145 |
| yolo11m | 0.703 / 0.533 | 0.606 / 0.460 vs 0.171 / 0.078 |
| **yolo26s** | **0.763 / 0.575** | 0.646 / 0.503 vs 0.628 / 0.412 |
| yolo26m | 0.713 / 0.551 | 0.637 / 0.501 vs 0.207 / 0.120 |

- test93 口径下 real93 基线的数字虚高（75/93 是它的训练图，如 yolo26s 0.857 / 0.587），仅作参考；**val18 才是双方都干净的对比口径**。
- 生成图 val（99 张）六权重 mAP50-95 全在 0.847~0.864，等价分不出优劣。

**结论**
1. **生成数据增益成立**：val18 干净口径 gen493 六权重全部优于 real93 基线（两轮均 6/6；含 yolo26s 0.646 vs 0.628）。
2. 正确表述是「**扩量有效、数量取胜**」而非「合成优于真实」：gen493 训练用 394 张生成图，基线只有 75 张真实图（约 5.3 倍量差）。危险样本无法真实收集，生成可不限量量产、靠数量取胜。
3. **验证路径**：① 再生成 500 张扩到约 1000 重训 → mAP 持续提升则数量取胜论更坚实；② 若提升停滞 → 250 vs 493 减半对比（有下降同样支撑量-效曲线）。
4. **推理 imgsz**：640 → 960 → 1280 → 1536 一路掉点（峰值 640），保持 640；「训练端 960」须重训后同 imgsz 评测才能验证（未跑）。
5. **选型**：本轮最佳 = yolo26s；两轮最佳不同（均在 s 级）→ 六权重等价、差异在噪声内，工程上选 s 级即可。

> 轮次沿革（一句话）：初版 500 张（2026-09-11）结论与修订版一致、六权重同样全胜；原始数字存档 `_trash\summaries\phase5_gen500_summary.json`。

**打标模型（当前生产件）**：`runs\detect\mix586_all\yolo26s\weights\best.pt`——586 张全量（493 生成 + 93 真实）从 COCO 预训练重训（约 16 min；自评 mAP50=0.994，train=val 虚高仅 sanity，人工复核兜底）。`autolabel.py` 与一键 bat 的默认模型。

## 四、路线图

1. ✅ 真实数据打底：93 张人工标注 + 分层划分（2026-09-02）。
2. ✅ 生成管线与首轮闭环：493 张走完「生成 → 预打标 → 人工复核 → 六权重训练 → 评测」（2026-09-14）。
3. **数量取胜验证（下一步）**：扩量（再生成 500 张 → 复核 → 累计重训）或 250 vs 493 减半对比；管线现成（`scripts\prepare_labeling_workspace.bat`）。
4. 多目标扩展：安全帽（第二目标，素材已起步）。
5. 论文：首投 Automation in Construction（elsarticle，2026-09-13 启动）；必补实验与写作状态见 `paper\PAPER_PLAN.md`（经典增广对照、公开数据对照、规模实验）。

## 五、资产与踩坑

- **环境**：`D:\yolo\.venv`（Py 3.13 / torch 2.8.0+cu129 / ultralytics 8.4.135）；GPU RTX 5070 Ti Laptop 11.9 GB（640/b16 无压力）；权重源 `weights\`（v8/11/26 × s/m）。
- **代码**：全部在 `scripts\`：`prepare_labeling_workspace.bat`（一键打标）、`autolabel.py`、`eval_gen493.py`、`run_phase3_gen493.ps1`、`splits_gen493\`、`mix586\`。
- **沙箱**：训练/评测/autolabel 需 danger-full-access；纯数据脚本 workspace-write 可跑；跨盘写（`D:\gas_cylinders`）也要升级。
- **踩坑**（完整表见 COMMANDS.md「已知坑」）：data.yaml 的 `path` 与清单行必须绝对路径；评测脚本须 `__main__` 守卫 + `workers=0`；打标全量重训自评虚高；`.gitignore` 不支持行内注释。

## 六、文件地图

| 路径 | 内容 |
|---|---|
| `PROGRESS.md` | **本文件**：唯一进度台账 |
| `COMMANDS.md` | 命令表（AI agent 用） |
| `Prompt.md` | 用户 prompt（私有，不入库） |
| `scripts\` | 全部代码（清单见第五节） |
| `annotator\` | 标注 GUI（`start_annotator.bat` / `stop_server.bat`） |
| `runs\detect\` | `real93\`（基线六权重）、`gen493\`（修订版六权重）、`mix586_all\`（当前打标模型） |
| `phase6_gen493_summary.json` | 结果汇总（权威数字） |
| `logs\` | 当前阶段日志（gen493 / mix586 / phase6） |
| `weights\` | 预训练源权重（v8/11/26 × s/m） |
| `paper\` | 论文工作区（PAPER_PLAN / working_paper.tex / references.bib） |
| `reference\` | 文献库（pdf / md / bib） |
| `_trash\` | **弃用文件归档区**（scripts / configs / summaries / runs / logs / data）；删前先移入，勿真删 |
| `D:\gas_cylinders\` | 数据区（仓库外），内容见第二节 |

% Generated by paper/make_figures.py --fig tab-cost.
% Source: logs/v4_<arm>_train.log; runs/detect/<run>/<weight>/results.csv
% Do not edit by hand; re-run the script so the numbers cannot drift from the source.
\begin{table}[t]
  \centering
  \small
  \caption{Training cost on one RTX 5070 Ti Laptop GPU, per run. ``Wall clock'' is the logged start-to-finish time of the whole run; the detector column is summed over the six detectors, so the difference between the two is the overhead outside the training loop. No run fell back to a smaller batch: batch 16 fit the card throughout.}
  \label{tab:cost}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{lrrcrr}
  \toprule
  Run & Images & Detectors & Wall clock & \multicolumn{2}{c}{Detector training time (min)} \\
    \cmidrule(lr){5-6}
     & & & & summed & range \\
  \midrule
  A & 93 & 6 & 1 h 16 m & 74 & 8--17 \\
  B & 1085 & 6 & 10 h 09 m & 602 & 69--131 \\
  C & 1085 & 6 & 10 h 06 m & 599 & 66--141 \\
  scale & 493 & 6 & 4 h 38 m & 273 & 31--63 \\
  \bottomrule
  \end{tabular}
\end{table}

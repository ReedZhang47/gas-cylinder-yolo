% Generated by paper/make_figures.py --fig tab-bootstrap.
% Source: experiments/v4_protocol/bootstrap_paired{,_L3}.json
% Do not edit by hand; re-run the script so the numbers cannot drift from the source.
\begin{table}[t]
  \centering
  \small
  \caption{Paired cluster bootstrap over the 61 development images (10\,000 resamples, seed 0, whole images resampled; both sides of a contrast are scored on the same resampled set). Each row is the arm-level joint detector+checkpoint estimate, so the detector is chosen inside the fit. $\Delta$ is the second arm minus the first for the arm contrasts, and 1085 minus 493 for the scale step. An interval that covers zero does not establish a difference: the two edited-synthesis contrasts and the scale step are clear of zero, while the classical-augmentation contrast against the real-only arm is not.}
  \label{tab:bootstrap}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{lcc}
  \toprule
  Contrast & $\Delta$ mAP50--95 [95\% CI] & $\Delta$ mAP50 [95\% CI] \\
  \midrule
  C $-$ A & +0.211 [+0.112, +0.320] & +0.163 [+0.058, +0.289] \\
  C $-$ B & +0.308 [+0.205, +0.407] & +0.178 [+0.041, +0.323] \\
  B $-$ A & -0.097 [-0.197, +0.017] & -0.015 [-0.160, +0.146] \\
  1085 $-$ 493, edited & +0.141 [+0.060, +0.249] & +0.100 [+0.012, +0.218] \\
  \bottomrule
  \end{tabular}
\end{table}

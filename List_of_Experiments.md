# List_of_Experiments.md — 待做实验一页清单（v3）

> 定位：实验操作清单。**实验线划分与设计见 `EXPERIMENTS.md`（总纲 / 新会话入口）**——L1 主线三臂对照、L2 方法学线 P2（验证 61val 口径）、L3 支撑线预算与噪声（已完成）。
> 维护规则：每跑完一项本表把「可以开始？」改为「✅ 已完成」；新实验往对应节加行。
> 「可以开始？」三档：**是**（数据/脚本现成）/ **半**（需先写脚本或有人工环节）/ **否**（等数据或等前置）。
> **v3 口径**：test = `new_test_set\`（61 张网络图，72 框，与训练数据完全独立）；三臂全量训练、只差训练数据来源；统一 **300 epoch** / imgsz 640 / batch 16 / `patience=0` / `save_period=10`，报 last.pt（61val 选权为 L2 对照口径）。

## 一、L1 主线：三臂同源对照（+ L2 协议对照，一次训练两种口径）

三臂设计（**同源**：A/B/C 都以 real93 为唯一数据来源，只差"怎么扩"）：

| 臂 | 训练数据（全量，不划自评 val） | 数据配置 |
|---|---|---|
| A 真实臂 | **93 张** | `D:\gas_cylinders\v3\data_real93.yaml` |
| B 真实+传统增广臂 | **1085 张**（93 张离线增广） | `D:\gas_cylinders\aug93\data_aug93.yaml`（脚本待写） |
| C 编辑合成臂 | **1085 张** | `scripts\splits_gen1085\data_gen1085_full_v3.yaml` |

| 实验名 | 通俗解释目的 | 做法 | 可以开始？ |
|---|---|---|---|
| B 臂：离线增广脚本 | 回答"免费的传统增广为什么不够" | 写 `scripts\make_aug93_dataset.py`：对 93 张做几何+色彩增广到 **1085 张**（与 C 臂按独立图片数对齐；**不配正负比**——标注单位是框、图上可混合），几何变换同步变框 → `D:\gas_cylinders\aug93\` | **是**（脚本 ≈1 h 开发，零新依赖优先 cv2/PIL） |
| L1+L2 成套训练（一次跑完） | 出三臂主表 + 61val 协议对照 | 三臂 × 六权重 × **300 轮**、`patience=0`、`save_period=10` → 每权重 30 个快照；`run_v3_arms.ps1 -Arm real93/aug93/gen1085` | **半**（等增广脚本） |
| L2 快照评测（每快照评 61 张） | 一条快照曲线读出 61test 与 61val 两种口径 | 复用 `scripts\p1_eval_snapshots.py` 的模式（改成按臂/权重遍历），产出选权增益表 | **半**（脚本需小改） |
| 主表汇总 | 论文主结果表 | L1 → 表 1（61test 口径）；L2 → 表 2（61val 口径 + 选权增益） | **半**（等训练） |
| ✅ C 臂旧口径复评（100 轮/868 张） | 历史数字，供对照 | 已完成（2026-09-19）：六权重在 61 张上 0.768~0.927 mAP50 / 0.555~0.698 mAP50-95 | ✅ 已完成（`phase10_v3_main.json`） |
| 规模曲线（**493 vs 1085，均 300 轮**） | 「合成数据也吃数量」——新口径 | 493 点：`scripts\splits_gen1085\data_gen493_full_v3.yaml`（batch-1 全量 493 张）→ 六权重 300 轮 → 评 61 张；**1085 点复用 L1 的 C 臂**（不额外训练） | **是**（GPU ≈3.3 h） |
| ~~394 vs 868 / 100 轮（旧口径）~~ | — | 已作废（口径改为 493 vs 1085 / 300 轮）；旧 run `gen493v3` 留档不再使用 | ⛔ 作废 |

## 二、方法学补充（按价值排序）

| 实验名 | 通俗解释目的 | 做法 | 可以开始？ |
|---|---|---|---|
| 复核价值消融（复核 vs 不复核） | 量化「人工复核」这道质量门值多少 mAP——管线贡献的直接证据 | 用打标模型对生成图重跑 autolabel 得「未复核」标签集 → 同权重训练 → 与已复核版对比 | **是**（纯推理 + 1~2 个权重训练） |
| 空标图消融 | 证明数据设计里「无违规场景」有用：少了它误报会涨 | 抽掉空标图重训，看 61 张上 FP 与 mAP 变化 | **是**（便宜） |
| 多随机种子重复 | 把「谁赢」从"看起来"变成"统计上" | A/C 两臂各补 2 个种子，报 mean±std。**不要用同源自评 val 选权提升指标**（实测在独立 test 上平均差 0.025） | **是**（便宜） |
| test 扩充（可选） | 61 张 / 72 框偏小，臂间差值容易被噪声吞掉（实测重训噪声 std ≈0.07） | 再收 40~60 张网络气瓶图（保持与源数据无关、走同一套复核）→ 扩后按 `check_test_independence.py` 重验并写回 `PROGRESS.md` 二.3 | **半**（需人工收图 + 复核） |
| 工作点/PR 曲线（免训练） | 给工地部署一个可用阈值 | 最终模型在 61 张上扫 conf，出 PR 曲线与推荐工作点 | **是**（分钟级） |
| 误差分析图（定性） | 论文必需的"为什么错"材料 | 最终模型在 61 张上分类 TP/FP/FN + 典型样例拼图 | **半**（等最终模型） |
| 分辨率敏感性 | 61 张分辨率跨度极大（192×137~2160×2880，中位 509×516，34/61 张两边都 <640） | 61 张上做 imgsz 扫描（640/960/1280） | **是**（分钟级） |

## 三、L3 支撑线：已完成（预算与噪声）

| 实验名 | 结论 | 状态 |
|---|---|---|
| epoch 预算诊断 | 100 轮时曲线仍在涨 → 预算不足 | ✅ |
| P1 快照判别（300 轮，30 快照） | 61test 0.3938 / 61val 上界 0.4487（虚高 +0.0549）；交叉验证选权增益 +0.0376；快照 std 0.1396 | ✅ |
| 饱和试点（450 轮） | 曾判"未饱和"（后被 750 轮推翻） | ✅ |
| 延长试点（750 轮） | **400 轮后无系统增益** → 统一 300 轮；自评不可判饱和；选权增益 +0.055~+0.058（三次独立训练） | ✅ |
| 重训噪声量化 | 同设置两次独立训练逐点差 std 0.067~0.086、中位绝对差 0.036~0.127 | ✅ |

原始数据：`experiments\p1_61val_vs_61test\`、`experiments\saturation_pilot\`；论证见 `PLAN_61VAL.md`。

## 四、第二目标（Hardhat，样本到手后）

| 实验名 | 通俗解释目的 | 做法 | 可以开始？ |
|---|---|---|---|
| 第二目标基本闭环 | 同一套流程跑通，证明管线可复制 | 生成 → 复核 → 训练 → 干净口径评测 | **否**（素材仅 2 张） |
| 跨目标迁移 | 方法级证据：编辑合成训出的模型，少量安全帽样本微调即可用 | 编辑合成预训练 → 少量安全帽样本微调 → 与从零训练对比 | **否** |
| 双目标统一模型 | 工程价值：一个模型同时管气瓶与安全帽 | 两类合成数据合并训单模型（双类）→ 分别评测 | **否** |

## 五、排期建议

1. **第一步**：写 B 臂增广脚本（≈1 h 开发）。
2. **第二步（一个 GPU 窗口，≈6 h）**：L1+L2 三臂 × 六权重 × 300 轮 + 快照评测 → 两张主表。
3. **第三步（≈3.3 h）**：规模曲线 493 点（1085 点复用上一步的 C 臂）。
4. **第四步**：方法学补充里最便宜的三项（复核价值、空标图、多种子）串行一个 GPU 窗口。
5. **第五步**：分辨率敏感性 + 工作点 + 误差分析（等最终模型定稿）。

## 六、附：创新点发掘方向（讨论稿）

1. **「状态级合成」而非对象级增广**：现有工作补对象本体（工人/PPE），本文补**违规状态**（未固定的放置姿态）；B 臂（传统增广）就是这条分界线的对照实验。
2. **把人工复核做成可量化的方法组件**：复核率 0%/50%/100% 的预算-性能曲线。
3. **独立测试集 + 可复现的独立性检验**：合成数据的编辑源就是种子照片，同源 test 必然泄漏；本文换用外部 test，并给出可复现的缩略图相关性检验（`check_test_independence.py`）。
4. **"选权还是末轮"的实测判据**：给出小样本下两种评测口径的增益、虚高与噪声量级（L2/L3 的直接产出）。
5. **真实感判据的可计算化**：real-vs-synth 判别器/真实感分数与下游 mAP 的关联。
6. **成本-性能前沿**：生图成本 + 复核分钟数 + 训练 GPU 时长 vs mAP。

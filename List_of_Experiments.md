# List_of_Experiments.md — 待做实验清单（v3）

> 怎么用这份清单：**第一节是"现在要跑的"**，跑完把状态改成 ✅；第二节是"以后可以加"；第三节是"已经跑完、不用再管"。
> 实验线的划分（主线 / 协议对照 / 支撑试点）见 `EXPERIMENTS.md`；命令见 `COMMANDS.md`；数字见 `PROGRESS.md`。
> 统一口径：**300 轮** / imgsz 640 / batch 16 / `patience=0` / 全量训练（不划自评 val）/ 报 `last.pt`；评估用 61 张网络图（`new_test_set`，72 框）。

---

## 一、现在要跑的（2 个批次）

### 0. 先写一个脚本（唯一的开发任务）

| 要做的事 | 做法 | 状态 |
|---|---|---|
| 写 `scripts\make_aug93_dataset.py` | 把 93 张真实照片**离线增广到 1085 张**（翻转 / ±10° 旋转 / 缩放平移 / 亮度对比度 HSV；几何变换同步改框；**不用上下翻转**，工地有重力方向）；只对齐总量，**不配正负比**（标注单位是框、一张图上可混合）→ 输出 `D:\gas_cylinders\aug93\` | ⬜ 待做（≈1 h） |

### 批次① 主线：三臂对比（回答"为什么用编辑合成，而不是免费的传统增广"）

| 臂 | 训练数据 | 规模 |
|---|---|---|
| A 真实基线 | real93 全量 | **93 张** |
| B 真实+传统增广 | 上面的增广脚本产出 | **1085 张** |
| C 编辑合成 | gen1085 全量 | **1085 张** |

做法：三臂各跑 **6 个权重 × 300 轮**（训练时每 10 轮自动存一个快照）→ 每个权重在 61 张上评一次 → 出两张表：

- **表 1（报末轮）**：现行口径，数字可直接当"泛化成绩"；
- **表 2（报快照最优）**：61val 口径，多一列"选权增益"。

| 状态 | 成本 |
|---|---|
| ⬜ 待跑（等增广脚本） | **≈6 h** 训练 + ≈40 min 评测 |

### 批次② 规模实验（回答"合成数据是不是也吃数量"）

| 点 | 训练数据 | 规模 |
|---|---|---|
| 小点 | gen493 全量 | **493 张** |
| 大点 | gen1085 全量 | **1085 张** ← **直接复用批次①的 C 臂，不重训** |

做法：小点跑 6 个权重 × 300 轮（同样存快照）→ 与批次①的 C 臂在同一批 61 张上比较 → 得到"493 → 1085"的增益。

| 状态 | 成本 |
|---|---|
| ⬜ 待跑（数据配置已就绪：`scripts\splits_gen1085\data_gen493_full_v3.yaml`） | **≈3.3 h** |

> 两批合计：**一个 ≈9.3 h 的 GPU 窗口**。跑完这两批，主结果表 + 规模曲线 + 61val 对照就全齐了。

---

## 二、跑完之后可以加的（都不急）

| 想做什么 | 为什么值得做 | 成本 |
|---|---|---|
| **规模曲线加第三点（gen250）** | 两个点只能连直线，三个点才能看出"收益递减" | 需先生成 250 张图（人工）+ 6 权重训练 |
| **扩充评估集（61 → ~120 框）** | 61 张偏小，臂间差值容易被噪声吞掉（实测重训噪声 std ≈0.07） | 人工收 40~60 张网络图 + 走一遍复核 |
| **多种子重复** | 把"谁赢"从"看起来"变成"统计上"（每个配置换随机种子再跑 2 次，报均值±标准差） | 便宜（s 级单次 ≈10 min） |
| **复核价值消融** | 量化"人工复核"这道质量门值多少 mAP——管线贡献的直接证据 | 推理 + 1~2 个权重训练 |
| **空标图消融** | 证明"无违规场景"的负样本有用（抽掉它，误报会涨） | 便宜 |
| **工作点 / PR 曲线** | 给工地部署一个可用阈值 | 分钟级（免训练） |
| **误差分析图** | 论文必需的"为什么错"材料（小图 / 遮挡 / 误报） | 等最终模型 |
| **分辨率敏感性** | 61 张分辨率跨度极大（192×137~2160×2880，中位 509×516），640 下小图可能吃亏 | 分钟级（imgsz 扫描） |

---

## 三、已经跑完、不用再管的（支撑实验）

这些是为了**定参数**才跑的，结论已写进 `EXPERIMENTS.md` 与 `PROGRESS.md`：

| 已完成的实验 | 结论（一句话） |
|---|---|
| 预算诊断（100 轮时曲线还在涨） | 100 轮训不够 |
| P1 快照判别（单权重 300 轮，30 个快照） | 选权比末轮好约 +0.04（交叉验证口径）；虚高上界 +0.055 |
| 450 轮试点 | 当时以为"没饱和"（后来被 750 轮推翻） |
| **750 轮试点** | **400 轮之后没有系统增益 → 统一用 300 轮**；自评曲线不能用来判饱和 |
| 重训噪声量化 | 同样设置训两次，逐点差 std ≈0.07、中位 0.04 → 论文里要写噪声量级 |

原始数据：`experiments\p1_61val_vs_61test\`、`experiments\saturation_pilot\`；详细论证：`PLAN_61VAL.md`。

---

## 四、暂时不跑的（第二目标，等素材）

| 想做什么 | 现状 |
|---|---|
| 安全帽基本闭环 / 跨目标迁移 / 双目标统一模型 | ⛔ 素材只有 2 张，等样本到手 |

---

## 五、建议的顺序

1. 写增广脚本 → 2. 批次①（≈6 h）→ 3. 批次②（≈3.3 h）→ 4. 可选里最便宜的三项（多种子、工作点、分辨率）串一个窗口 → 5. 等最终模型定稿后画误差分析图。

# List_of_Experiments.md — 待做实验一页清单（v3）

> 定位：实验操作清单。
> 维护规则：每跑完一项本表把「可以开始？」改为「✅ 已完成」；新实验往对应节加行。
> 「可以开始？」三档：**是**（数据/脚本现成）/ **半**（需先写脚本或有人工环节）/ **否**（等数据或等前置）。
> **v3 口径**：test = `new_test_set\`（61 张网络图，72 框，与训练数据完全独立）；各臂只差训练数据来源；统一超参 100 epoch / imgsz 640 / batch 16，**固定轮数报 last.pt、不做验证集选权**。

## 一、主实验：三臂对照 + 规模曲线

三臂设计（**同源对照**：A/B/C 都以 real93 为唯一数据来源，只差"怎么扩"）：

| 臂 | 训练数据 | 说明 |
|---|---|---|
| A 真实臂 | real93 全量 **93 张** | 原始真实数据基线 |
| B 真实+传统增广臂 | 离线增广到 **868 张** | 翻转/旋转/缩放平移/亮度对比度 HSV；**不用上下翻转**（工地场景有重力方向）；**只对齐总量，不配正负比**（标注单位是框） |
| C 编辑合成臂 | gen1085 train **868 张** | Qwen-Image-Edit 编辑合成的违规状态样本 |

| 实验名 | 通俗解释目的 | 做法 | 可以开始？ |
|---|---|---|---|
| A 臂训练（真实基线） | v3 的地板：只给 93 张真实图能到什么水平 | `v3\data_real93.yaml`（train=val=93，test=新 test）→ 六权重 100 轮 patience=0 → `eval` 新 test | **是**（GPU ≈1 h） |
| B 臂：离线增广脚本 | 回答"免费的传统增广为什么不够" | 写 `scripts\make_aug93_dataset.py`：对 93 张做几何+色彩增广到 **868 张（总量与 C 臂一致即可；不按"单图有无标签"配正负比——标注单位是框、图上可混合）**，几何变换同步变框 → `D:\gas_cylinders\aug93\` | **是**（脚本 ≈1 h 开发，零新依赖优先 cv2/PIL） |
| B 臂训练 | 同上，出数字 | 同 A 臂协议六权重 → 评测新 test | **半**（等增广脚本） |
| ✅ C 臂成套复评 | 合成臂的完整数字 | 已完成（2026-09-19）：六权重在新 test 上 **0.768~0.927 mAP50 / 0.555~0.698 mAP50-95**，最优 yolo26s（0.927 / 0.698） | ✅ 已完成 |
| 主表汇总 | 论文主结果表 | 三臂 × 六权重 × 新 test → `phase10_v3_main.json` + 汇总表（表格预留 A/B/C 三列，B 列待填） | **半**（等 A/B） |
| ✅ 规模曲线（394 vs 868） | 「合成数据也吃数量」 | 已完成：gen493（394）在新 test 复评 → **六权重全涨 +0.07~+0.15 mAP50-95**（yolo26s 0.557→0.698） | ✅ 已完成（`phase10_v3_main.json`） |
| gen493 重跑（协议统一） | 让规模曲线两点**同协议**可比 | 现役 gen493 权重是 v1 时期训练（`patience=100`、其 data.yaml 的 test 指 v1_split）；新建 v3 版 data.yaml（test → `v3\test61.txt`）、train 清单沿用 `Placement_Issues\gen493_split\`（394 张）→ 六权重 100 轮 patience=0 → 复评新 test | **是**（GPU ≈1 h；注意 `phase10_v3_main.json` 的 gen493 数字届时会更新） |

## 二、方法学补充（按价值排序）

| 实验名 | 通俗解释目的 | 做法 | 可以开始？ |
|---|---|---|---|
| 复核价值消融（复核 vs 不复核） | 量化「人工复核」这道质量门值多少 mAP——管线贡献的直接证据 | 用打标模型对生成图重跑 autolabel 得「未复核」标签集 → 同权重训练 → 与已复核版对比 | **是**（纯推理 + 1~2 个权重训练） |
| 空标图消融 | 证明数据设计里「无违规场景」有用：少了它误报会涨 | 抽掉空标图重训，看新 test 上 FP 与 mAP 变化 | **是**（便宜） |
| 多随机种子重复 | 把「谁赢」从"看起来"变成"统计上" | A/C 两臂各补 2 个种子，报 mean±std（s 级单次 ≈10 min） | **是**（便宜） |
| 工作点/PR 曲线（免训练） | 给工地部署一个可用阈值 + 展示 precision/recall 权衡 | 最终模型在新 test 上扫 conf，出 PR 曲线与推荐工作点 | **是**（分钟级） |
| 误差分析图（定性） | 论文必需的"为什么错"材料（小图/遮挡/误报） | 最终模型在新 test 上分类 TP/FP/FN + 典型样例拼图 | **半**（等最终模型） |
| 分辨率敏感性（重要） | 新 test 分辨率跨度极大（192×137~2160×2880，中位 509×516，34/61 张两边都 <640），640 下小图可能吃亏 | 新 test 上做 imgsz 扫描（640/960/1280），必要时补充结论与建议工作点 | **是**（分钟级） |

## 三、第二目标（Hardhat，样本到手后）

| 实验名 | 通俗解释目的 | 做法 | 可以开始？ |
|---|---|---|---|
| 第二目标基本闭环 | 同一套流程跑通，证明管线可复制 | 生成 → 复核 → 训练 → 干净口径评测 | **否**（素材仅 2 张） |
| 跨目标迁移 | 方法级证据：编辑合成数据训出的模型，少量安全帽样本微调即可用 | 编辑合成预训练 → 少量安全帽样本微调 → 与从零训练对比 | **否** |
| 双目标统一模型 | 工程价值：一个模型同时管气瓶与安全帽 | 两类合成数据合并训单模型（双类）→ 分别评测 | **否** |

## 四、排期建议

1. **已完成**：C 臂六权重复评 + 规模曲线复评（2026-09-19，均为评测，分钟级）。
2. **下一步（一个 GPU 窗口）**：A 臂训练（1 h）→ gen493 按 v3 协议重跑（1 h）→ 写增广脚本 → B 臂训练（2.5 h）→ 主表汇总。重跑 A 臂前先删 `runs\detect\real93v3`。
3. **第三步**：方法学补充里最便宜的三项（复核价值、负样本、多种子）串行一个 GPU 窗口。
4. **第四步**：分辨率敏感性 + 工作点 + 误差分析（等最终模型定稿）。

## 五、附：创新点发掘方向（讨论稿）

1. **「状态级合成」而非对象级增广**：现有工作补对象本体（工人/PPE），本文补**违规状态**（未固定的放置姿态）；B 臂（传统增广）就是这条分界线的对照实验。
2. **把人工复核做成可量化的方法组件**：复核率 0%/50%/100% 的预算-性能曲线，回答"复核值多少 mAP、该花多少复核成本"。
3. **独立测试集 + 可复现的独立性检验**：稀缺目标的数据扩增论文常用"留出真实照片"做 test，但合成数据一旦由这些照片编辑而来就天然泄漏。本文的做法是**换用与源数据完全无关的外部 test 集**（61 张网络图片），并给出**可直接复现的独立性检验**（缩略图相关性：test↔real93 0.730 / test↔生成图 0.732，对照 real93 内部 0.834，脚本 `scripts\check_test_independence.py`）——把"独立性"从声明变成一次可跑的检查。
4. **真实感判据的可计算化**：real-vs-synth 判别器/真实感分数与下游 mAP 的关联，给出可操作的"生成质量闸门"（防「过净」域差）。
5. **成本-性能前沿**：生图成本 + 复核分钟数 + 训练 GPU 时长 vs mAP，回答"给定预算怎么分配"。
6. **发布数据集/协议**：复核后的合成数据集 + 评测协议 + 权重公开，配 arXiv 预印本。

# List_of_Experiments.md — 待做实验一页清单（2026-09-17 立）

> 定位：把 `paper\PAPER_PLAN.md` 第四节（必补实验）+ 项目讨论里散落的实验点子，整理成一张**可勾选的操作清单**。写完论文前**只改这张表和 PROGRESS.md**，不用来回翻计划文件。
> 维护规则：每跑完一项 → 数字进 summary JSON 并把结果一句话回写 `PROGRESS.md`；本表把对应行的「可以开始？」改为「✅ 已完成」；新增实验往第二节加行并标【新增】。
> 「可以开始？」三档：**是**（数据/脚本现成）/ **半**（有人工环节或需先写小脚本）/ **否**（等数据或等前置实验）。
> 已完成、不在本清单的基础结果：gen493 六权重主结果、real93 基线、imgsz 扫描（见 `phase6_gen493_summary.json`）、打标模型 mix586_all（v1 口径的数字在 v2 切换后重算）。

> ⚠️ **2026-09-18 重大数据问题**：审计发现 1085 张生成图**全部是真实照片的 Qwen-Image-Edit 编辑版**，93 张真实照片全被用作编辑源，其中 611 张（56%）源于 test33 → **生成臂训练池污染，全部 gen 结果作废重做**（real60/real93 基线不受影响）。无泄漏池 `D:\gas_cylinders\gen_clean\`（474 张）已另存；审计工具 `scripts\make_gen_clean.py`。**任何训练/验证池都必须先过这道过滤**（源图 ∈ test33 的样本不得入池）。

> **2026-09-17 口径变更（v2 真实集划分，方案 A 已批准）**：93 张真实照片重新分层划分为 **train 60（37 正 + 23 负）/ test 33（20 正 + 13 负）**；**test33（33 图 / 约 60 框）取代 v1 的 val18（18 图 / 30 框）成为唯一干净口径**——任何模型不训练、不选权、不碰；全篇选权协议统一为**固定 100 epoch、报最后一轮（last.pt）、不做验证集选权**（实测 best vs last 差 ≤0.02、同规则比较结论不变）。主表 gen 权重按 last.pt 重评（分钟级）；真实基线按 train60 重训（=三臂 A 臂，一次两用）。

## 一、计划内实验（PAPER_PLAN 第四节原定）

| 实验名 | 通俗解释目的 | 做法 | 可以开始？ |
|---|---|---|---|
| ✅【前置】真实集 v2 重划 + 主表口径切换（方案 A） | 把干净测试集从 18 图扩到 33 图，并让全篇只有一个评测协议 | 已执行（2026-09-18）：`splits_real_v2\make_split_real_v2.py` → train60（37 正 + 23 负，113 框）/ test33（20 正 + 13 负，66 框）；`run_phase7_real60.ps1` 训真实基线六权重；`eval_realv2.py` → `phase7_realv2_summary.json`。结果：**gen 在 test33 上 6/6 全胜（+0.28~+0.48 mAP50-95）** | ✅ 已完成（数字见 PROGRESS 三；m 系列真实臂欠训练，诊断中） |
| 【作废重做】累计扩量重训（493+592=1085） | 「数据越多越准」这条曲线的第二个点 | 2026-09-18 污染版已完成但作废。**无泄漏版已出第一组数**：gen_clean（474 池 / train 379）→ yolo26s → test33 **0.611 / 0.377**，对 real60（0.479 / 0.268）仍 +0.13 / +0.11，但远小于污染版的 0.842/0.674（`phase9_genclean_yolo26s.json`）。**待办**：多权重+多种子复现；路线 A（gen_clean 全量）或 B（另收新 test 后可复用 1085 张）择一 | 🔄 单权重已跑通，待成套复现 |
| 规模消融·减半对比（250 vs 493） | 曲线的反方向验证：数据砍一半，成绩应当掉——这样才有把握说「涨分是数量的功劳」 | 从第一批 493 中按正/负比例抽约 250 张建新训练集（其余不参与或作 val），同超参重训（s 家族 2~3 个权重），评测用 v2 口径（test93 全量 + test33 干净） | **是**（数据现成，GPU 约 1~2 h） |
| 经典增广三臂对照（A 真实60 / B 增广600 / C 生成600） | 回答「传统增广免费管够，为什么还要费劲生成」：证明传统增广能补量、补不了多样性（新场景） | A=60 张真实图（v2 train60）；B=60×10 离线增广=600（370 正 + 230 负；hflip、±10° 旋转、缩放平移、亮度/对比度/HSV，**不用上下翻转**，几何变换同步变框）；C=从 1085 池按 370 正 + 230 负同比例抽 600（固定 seed）；三臂同超参（100 epoch / 640 / b16）+ 固定轮数报 last.pt，统一在 v2 test33 上评测 | **是**（需先写增广/抽样脚本 ≈1 h；GPU ≈3 h；A 臂与主表真实基线是同一批训练） |
| 混合训练口径（生成 + 真实） | 回答审稿人必问的「为什么不把真实图也混进来训？」 | 用 394 生成 + 60 真实（v2 train60）混合 train 重训，test 仍用 v2 test33（与 train60 不相交，保持干净） | **是**（便宜；口径要小心） |
| 安全帽第二目标（跨目标闭环） | 证明这套管线不是只为气瓶定制：换个稀缺目标照样跑通 | 先生成 200~500 张安全帽样本 → 复核 → 训练 → 与真实基线同口径对比（test = 留出的真实安全帽图） | **否**（素材仅 `Hardhat\` 2 张，需先生成+复核） |

## 二、建议补做（2026-09-17 新增，按价值排序）

| 实验名 | 通俗解释目的 | 做法 | 可以开始？ |
|---|---|---|---|
| ✅【新增·2026-09-18】真实臂训练预算对照（100 vs 300 轮） | 回应最可能被审稿人攻击的点「你的真实基线是不是欠训练」 | 已执行：real60 六权重 300 轮（`run_phase7_real60.ps1 -Epochs 300 -RunPrefix real60_300ep`）→ `eval_real60_300ep.py` → `phase7_300ep_check.json`。**结果：干净 test33 上几乎不变（s 系列 ±0.02、m 系列仍只有生成臂零头）；污染 test93 上虚高放大（+0.11~+0.33）→ 主结论不依赖训练预算，v2 的 100 轮协议维持** | ✅ 已完成（同时成为论文 C2「泄露随训练预算放大」的证据） |
| 【新增】复核价值消融（复核 vs 不复核） | 量化「人工复核」这道质量门到底值多少 mAP——这是管线贡献最直接的证据，也回应「自动标注有错怎么办」 | 用现有 mix586 打标模型对第一批 493 张重跑 autolabel（`--overwrite` 写到临时 labels 副本）得「未复核」标签集 → 同权重训练 → 与已复核版对比（注意：这等于让对照组用**更强的打标模型**，结论偏保守，更稳） | **是**（纯推理 + 1~2 个权重训练，便宜） |
| 【新增】负样本消融 | 证明数据设计里「负样本（规范放置 / 空场景）」有用：少了它，误报会涨 | 训练时抽掉空标（负样本）图，只留正样本重训，看 test93 上误报（FP）与 mAP 变化 | **是**（纯数据操作 + 1 个权重训练，便宜） |
| 【新增】多随机种子重复 | 把「6/6 全胜」从「看起来赢了」变成「统计上赢了」，堵住「碰运气」的质疑 | 取最佳权重 yolo26s 和真实基线，各换 2 个随机种子重训，在 v2 test33 上报 mean±std（s 级单次约 12 min） | **是**（便宜，可与其它实验共用 GPU 窗口） |
| 【新增】打标模型迭代收益（数据飞轮） | 量化「复核后的数据反过来喂给打标模型，下一批复核更省力」——闭环自我升级的证据，方法学亮点 | 现在做第一个点：用 mix586 给第二批 592 张重新打标，与人工复核后的标签逐框比对（编辑率 / 漏检率 / 误检率）；等 1085 模型训好后再测第二个点 → 得到「打标模型每升级一代、编辑率下降多少」的曲线 | **是**（纯推理，分钟级） |
| 【新增】定量评测补充（免训练） | 论文补两张便宜的图：① 部署工作点（PR 曲线，工地用多大阈值）；② 小目标切片推理是否补分 | ① 用最终最佳模型在 test33 / test93 扫 conf，出 PR 曲线与推荐工作点；② 在 test93 上对比整图 640 vs 切片推理（SAHI 或自实现滑窗；新依赖先确认再装） | **是**（纯评测，分钟级） |
| 【新增】误差分析图（定性） | 论文必需的「为什么错」材料：小目标漏检、遮挡、过净伪影各占多少 | 用最终最佳模型在 test93 上把逐框判成 TP / FP / FN，分类归因 + 典型样例拼图（Fig. 用例） | **是**（等最终模型定稿后做，便宜） |
| 【新增】生成方式对照（文生图 vs 编辑真实照片） | 管线里写了 qwen-image-edit 分支但实验只用文生图；补上才能撑「管线完整」的主张（背景真实感 vs 可控性） | 用 qwen-image-edit 基于 93 张真实照片生成 100~200 张 → 人工复核 → 单独训练或与文生图混合对照 | **半**（生成与复核是人工环节；建议先小规模 100 张试水） |
| 【新增】真实测试集扩容 | 93 张测试集来源单一，「泛化性」会被质疑；更多真实照片 = 更硬的结论 | 收集不同工地 / 光照 / 角度 / 相机的真实巡检照片（含成为第二测试集的可能），用现有权重直接评测 | **否**（需要用户收集照片；**注意：若近期能收到新照片，应在执行 v2 划分之前收齐**——v2 只划一次，划完再补就要重划重训） |

## 三、第二目标（Hardhat）样本到手后

| 实验名 | 通俗解释目的 | 做法 | 可以开始？ |
|---|---|---|---|
| 第二目标基本闭环 | 与气瓶同一套流程跑通，证明管线可复制 | 生成 → 预打标 → 复核 → 训练 → 干净口径评测；test = 留出的真实安全帽图 | **否**（等样本） |
| 【新增】跨目标迁移（预训练先行） | 最强的方法级证据：气瓶生成数据训出的模型，拿来微调安全帽只花很少样本就超过「从零训练」→ 生成数据的价值可跨目标迁移 | 气瓶生成数据预训练权重 → 少量安全帽样本（真实或生成）微调 → 与从 COCO 预训练直接训的基线对比（同 test） | **否**（等样本） |
| 【新增】双目标统一模型 | 工程价值 + 通用性：一个模型同时管气瓶放置与安全帽佩戴 | 两类生成数据合并训单模型（双类），在两组 test 上分别评测，与单目标模型对比 | **否**（等样本） |

## 四、排期建议（先做什么）

0. **第零步：真实集 v2 重划 + 主表口径切换（前置，2026-09-17 方案 A）**——先把 test33 与 last.pt 协议落地，后面所有实验才有统一口径。若近期还能收到新真实照片，**先收齐再划**。
1. **第一步：合并划分 + 1085 重训**——它决定后续故事怎么写（曲线还涨 → 主打扩量；停滞 → 主打减半对比与增广对照），数据已就绪。
2. **第二步（同一个 GPU 窗口串行）**：经典增广三臂对照、减半对比、复核价值、负样本消融、多种子——脚本化串行跑完，数字一次性入账（三臂 A 臂与主表真实基线是同一批训练，不重复算）。
3. **第三步**：生成方式对照（人工环节最重，放最后或按投稿时间取舍）。
4. 免训练的补充评测（工作点 PR、切片推理、误差分析图）等最终模型定稿后一次性做。

## 五、附：论文创新点还能往哪发掘（讨论稿，不是实验清单）

1. **把「人工复核」从流程细节升级为可量化的方法组件**：现有 auto-labeling 文献很少量化「人工复核值多少 mAP / 复核多少比例够用」。若补「复核率 0% / 50% / 100%」的预算-性能曲线，就从「我们有个流程」变成「我们知道该花多少复核成本」——第二节的复核价值消融是它的第一块砖。
2. **数据飞轮：打标模型自迭代闭环**：每批复核后的数据升级打标模型，下一代打标质量更好、复核更省力（编辑率曲线）。把「闭环」写成可测的收敛过程（每代编辑率/漏检率），是方法学上少见的角度，且测量近乎免费（纯推理）。
3. **「状态稀缺」而非「对象稀缺」的问题定义**：现有合成数据工作大多补**对象本体**（工人、PPE、工件），本文补的是**违规状态**（未固定的放置姿态）——可补一个分界实验：对象级合成（copy-paste / ControlNet 把气瓶贴进真实照片的常规位置）vs 状态级生成（本管线），证明前者解决不了违规状态。这个对照同时把最接近的三篇竞品（BlendCon、LoRA 生成、real-synth mixing）全部划到界外。
4. **真实感判据的可计算化（v0「过净」教训的量化）**：训练一个 real-vs-synthetic 判别器或算 CLIP/美学分数，把每张生成图打个「真实感分」，再关联下游 mAP——给出一个可操作的「生成质量闸门」（哪些图该丢弃）。把工程教训升级成指标，Discussion 会从经验谈变成方法。
5. **固定预算下的成本-性能前沿**：把「生图成本 + 人工复核分钟数 + 训练 GPU 时长」和 mAP 画在同一张图上，回答「给定预算，多生成还是多复核」。应用刊（AiC）很吃这套务实叙事，数据从现有实验里就能凑。
6. **发布数据集 / 协议本身作为贡献**：复核后的合成数据集 + 评测协议 + 权重公开（附 arXiv 预印本）——不是实验，但显著影响引用与审稿印象，成本只是整理成本。

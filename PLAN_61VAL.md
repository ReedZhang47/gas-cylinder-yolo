# PLAN_61VAL.md — 「61val 方案」讨论记录与待评估实验（2026-09-20 立）

> 定位：**讨论记录 + 待评估实验台账**，不是定稿方案。**实验线总纲见 `EXPERIMENTS.md`**（本文件对应其中的 L2 方法学线与 L3 支撑线）。本文件记录 v3 现行评测口径（下称 **61test 方案**）之外的一个候选口径（下称 **61val 方案**），以及判别它是否值得采用的实验。
> 与 `PROGRESS.md`（唯一进度台账）的关系：本文件记录"方案讨论与判别实验"；结论一旦落定，写回 `PROGRESS.md` 与 `paper\PAPER_PLAN.md`。
> 实验条目同步登记在 `List_of_Experiments.md`。

## 一、两个方案的定义

| | **61test 方案**（现行 v3 口径） | **61val 方案**（候选） |
|---|---|---|
| 训练数据 | 各臂**划分后**的子集：A 93（全量）/ gen493 train 394 / gen1085 train 868 | 各臂**全量**：A 93 / gen493 **493** / gen1085 **1085**；B 臂离线增广量随之对齐到 **1085** |
| 自评 val | 从各自数据里划出（A train=val=93；gen1085 val 217；gen493 val 99），只用于打印曲线 | **不划**；yaml 的 `val:` 指向 train 自己（仅为满足 ultralytics 要求，不用于任何决策） |
| 选权 | **不选权**：固定 100 轮、报 `last.pt`（`patience=0`） | **在 61 张网络图上选权**：取 val 上最优 epoch 的 `best.pt` |
| 报告的成绩 | `last.pt` 在 61 张网络图上的 mAP | `best.pt` 在**同一批 61 张**上的 mAP |
| 61 张网络图的角色 | **test**（唯一评测口径，不参与任何训练/选权） | **val**（选权 + 报数）；test 概念后移到真实工程现场 |
| 是否回答"泛化/域差" | 能（test 未被碰过） | 不能（报数集即选权集，无独立读出） |

## 二、提出者主张成立的部分

1. **全量训练**：为一个"只用来打印"的自评 val 而砍掉 21~25% 训练数据（A 臂 93→若划 val 只剩 ~73；gen1085 1085→868）不合算。固定轮数协议下 val 不参与任何决策，代价是纯浪费。**这条独立于选权问题成立**：即使最终仍报 last.pt，也不该为自评而砍数据。
2. **预算加大后选权从"锦上添花"变成"止损"**：若把 epoch 提到 300，末轮大概率已越过峰值，`last.pt` 变成主动上报一个过拟合检查点；此时"在 val 上选权"能保证不选到最差的那一端。
3. **61val 的选权集合来源更干净**：那 61 张是**独立网络图**，而现行 `best.pt` 的选权集合是**同源合成域自评 val**。在独立集上选权，起码不是在同源集合上过拟合。（讨论中曾用"自评 val 选出的 best.pt 在 test 上平均更差 0.025"来反对选权 —— **该论据只适用于自评 val，不适用于 61val 方案**，见 `PROGRESS.md` 五节坑 7。）

## 三、仍需量化的缺陷（不反对，但必须给数）

1. **报告值必然虚高**：报的是"72 个框上 N 个候选取最大"，属 model-selection metric / 上界，不是泛化估计。论文必须如此声明。
2. **放弃"泛化/域差"这一读数**：无独立 test，无法回答"编辑合成学到的是违规状态还是合成域外观"。→ **两个方案并存，不是替代**：61test 回答泛化，61val 回答"全量训练 + 选权"下的比较。
3. **61 张上的最优轮是否稳定**：72 框、候选多，最优轮可能由噪声主导；但提出者指出**最优轮必有区间**，且 val 质量再差也"止点损"（不会选到远端的过拟合轮）。此点需实测。
4. **yaml 的 `val:` 不得指向 61 张**：否则训练日志会每轮打印 benchmark 指标，等于训练过程偷看答案。指向 train 自己即可（当前 `patience=0` 下 val 不参与决策）。

## 四·五、P1 实测结果（2026-09-20 执行完毕）

**设置**：A 臂 real93 全量 **93 张** + `yolo26s`，**300 轮**（`patience=0`、`save_period=10`、imgsz 640、batch 16）；30 个快照（epoch 10…300）每个都在 **61 张网络图**上评一次。原始数据：`runs\detect\p1_61val_vs_61test.json`（快照曲线）+ `runs\detect\p1_cross_split.json`（交叉验证）；脚本 `scripts\p1_run.ps1` / `p1_eval_snapshots.py` / `p1_cross_split.py`。

**三条口径的值**：

| 口径 | 值（61 张上） |
|---|---|
| **61test**（末轮 epoch300） | mAP50 **0.6214** / mAP50-95 **0.3938** |
| **61val**（最优快照 epoch290，选权 + 报数同集合＝上界） | mAP50 **0.6928** / mAP50-95 **0.4487** |
| 上界虚高 | mAP50 **+0.0713** / mAP50-95 **+0.0549** |

**交叉验证（"用一半图片选权、在另一半上报数"——61val 方案真实存在的偏差）**：

| 选权用哪半 | 选出 epoch | 在另一半上：选中的 | 末轮(300) | 选权增益 |
|---|---|---|---|---|
| 前一半 31 张 | 290 | 0.4038 | 0.3908 | **+0.0130** |
| 后一半 30 张 | 250 | 0.4573 | 0.3951 | **+0.0622** |
| | | | **均值** | **+0.0376** |

**稳定性**：两半选出的最优轮不同（290 vs 250），但两侧的读数差不大（选权增益 +0.013 ~ +0.062）→ **选权区间确实存在且集中在中后段**（提出者的判断成立），但"选到哪个 epoch"由噪声决定。30 个快照的 mAP50-95 极差 0.0003~0.4487、**std 0.1396**——**检查点选择的方差远大于臂间差值**。

**自评 val 曲线对照**（yaml 的 `val:` 指向 93 张训练图自己）：自评最优轮 **276**，末轮 0.9003，末 20 轮斜率 **−0.0023/10 轮（已走平）**；末 10 轮自评只掉 0.0047，而 61 张上掉 0.0382。→ **"过拟合"发生在自评口径看不到的地方**，这正是末轮协议的真实风险。

### 结论（对两个方案）

1. **61val 方案的价值被证实**：选权相对末轮有**平均 +0.038 mAP50-95** 的真实增益（交叉验证口径，非上界），因为曲线在 250~290 一带明显优于 300。
2. **虚高必须声明**：报数是"选权集合上的最大值"，上界虚高 **+0.055 mAP50-95 / +0.071 mAP50**；论文里只能作为 model-selection metric，不能当泛化估计。
3. **全量训练**：本设置下 93 张全训 300 轮在 61 张上已超过 868 张训练 100 轮的 C 臂（0.4487 vs 0.3938，**但预算不同、不可直接比较**，只说明"数据量"不是唯一甚至不是当下的主要约束）。
4. **300 轮仍未饱和**：曲线在 250~290 还创出新高，末段波动剧烈。要定稿主表，建议三臂同预算（**且预算 > 300**，需先做 300/450 试点判饱和）。


## 五、判别实验设计与状态

> 讨论中曾计划 G1（在 gen1085/yolo26s 的 100 轮快照上量选择偏差）与 G2（全量 vs 划分的增益）——**两者均被否决**：G1 应在"真会过拟合"的 300 轮设置下测；G2 结论已定（图已生成，没理由浪费数据）。

### P1（✅ 已完成 2026-09-20 —— 设置、口径与结果见四·五节）

**设置**：用 **A 臂（real93 全量 93 张）+ yolo26s** 训练 **300 轮**（`patience=0`），**每 10 轮存一个快照**（30 个检查点）；每个快照在 **61 张网络图**上评一次 mAP。

**同时回答三件事**：

1. **61test 口径**：末轮（epoch 300）在 61 张上的值 ↔ 现行 61test 方案的数字（它必然选 epoch 300）。
2. **61val 口径**：30 个快照在 61 张上的最大值（= 61val 方案会报的数）↔ 末轮值 → **选权收益 / 虚高幅度**。
3. **过拟合是否真的发生、发生在第几轮**：快照曲线（以及自评 val 曲线）的峰值位置与形状 → 决定"300 轮是否必要""选权是否真的在止损"。

**附带产出**：**选权稳定性**——用不同子集（61 张的前后两半）各选一次最优轮，报告最优轮差异，以及"在另一半上报数"的真实选择偏差。

**实际成本**：训练 ≈13 min（93 张、300 轮）+ 30 次快照评测 + 18 次交叉评测 ≈15 min。

### P2（待定，视四·五节结论）

P1 已证明 300 轮下选权有真实止损价值（均值 +0.038），因此 P2 的方向成立：三臂统一升到 **同预算**（建议 >300，先做饱和试点）+ 全量训练 + 61val 选权，作为 61val 方案主表；同时保留 61test 方案数字作为泛化读数。注意 `close_mosaic` 占比随总轮数变化（100 轮 10% vs 300 轮 3.3%），需声明或按比例对齐。

## 六、饱和试点结果（2026-09-20 执行完毕）

**设置**：与 P1 完全相同的设置（A 臂 real93 全量 93 张 + `yolo26s`、`patience=0`、`save_period=10`、imgsz 640、batch 16），只把总轮数从 300 加到 **450**；45 个快照全部在 61 张上评测。原始数据 `runs\detect\pilot_saturation.json`；脚本 `scripts\pilot_saturation.ps1` / `pilot_saturation_eval.py`。

| 指标 | 100→200 | 200→300 | 300→400 | 400→450 |
|---|---|---|---|---|
| 61 张上的 mAP50-95 增益 | **+0.1360** | +0.0026 | **+0.0435** | （450 轮未到 10 的倍数取样点，见末轮） |
| 自评 val 上的 mAP50-95 增益 | +0.1291 | +0.0747 | +0.0654 | **−0.0040** |

- **61 张上的平滑曲线到 420 轮仍在创新高**（3 点平滑：300 轮 0.4653 → 330 轮 0.4767 → 360 轮 0.4893 → 390 轮 0.4996 → 420 轮 0.5193），**没有饱和**。
- **末轮 vs 最优快照**：末轮（epoch450）mAP50-95 **0.4684**，最优快照 epoch430 **0.5259** → 选权增益 **+0.0575**（与 P1 在 300 轮下的 +0.0549 同量级，**选权止损的结论在更长预算下复现**）。
- **自评 val 会误导**：它在 400~450 段已经走平甚至下降（−0.0040），而 61 张上同一段还在涨。→ **不能用自评曲线判断"训够了没有"**。

### 意外的关键副产物：重训噪声的直接估计

P1（300 轮）与本试点（450 轮）是**两次独立从头训练**，在共享的 30 个 epoch 上有逐点可比的 61 张读数：

| 统计量（mAP50-95 差值：pilot − P1） | 值 |
|---|---|
| 全部 30 个 epoch 均值 | −0.0047 |
| 标准差 | **0.0670**（epoch ≥150 时 0.0741） |
| 极差 | −0.2284 … +0.1187 |
| 中位绝对差（epoch ≥150） | **0.0356** |

→ **两次相同设置的训练，逐点差异中位数就有 0.036、极差可达 0.23，而曲线是平滑上升的（均值仅 −0.005）**。这条直接回答了一个悬而未决的问题：**单次末轮读数不可靠，臂间差值（当前 +0.07~+0.15）虽然大于噪声中位数，但同一量级**。论文里必须用"选权/多轮平均/多种子"来压这个噪声，并给出噪声量级。

### 结论

1. **450 轮仍未饱和**：主表预算不能定在 300，也不宜定在 450；建议先做 **450→750 的延长试点**（同一权重、同设置，只加轮数，直接看尾部是否走平）。
2. **选权必须做**：300 轮下 +0.055、450 轮下 +0.058，两次独立复现；末轮确实会落在偏差的检查点上。
3. **必须报噪声量级**：重训逐点 std ≈0.067、中位绝对差 0.036 —— 这应作为论文的方法学声明（"我们的臂间差值约 X，重训噪声约 Y"）。

## 七、延长试点结果（2026-09-20，750 轮）—— **结论反转：曲线在 400 轮后不再系统性上升**

**设置**：与前两次完全一致（A 臂 real93 全量 93 张 + `yolo26s`、`patience=0`、`save_period=10`），总轮数 450→**750**；稀疏评测 15 个点（10/50/100 + 150~750 每 50 轮）。原始数据 `runs\detect\pilot_saturation_750.json`；脚本 `scripts\pilot_sat750.ps1` / `pilot750_eval.py`。三次独立训练合看（同数据、同权重、同超参，只有预算不同）：

| 预算 | 末轮 mAP50-95 | 最优快照 | 选权增益 |
|---|---|---|---|
| 300 | 0.3938 | 0.4487 (ep290) | +0.0549 |
| 450 | 0.4684 | 0.5259 (ep430) | +0.0575 |
| 750 | 0.3782 | 0.4358 (ep700) | +0.0576 |

**61 张上的分段增益（750 轮那次）**：100→200 +0.036、200→300 −0.027、300→400 **+0.152**、400→500 −0.027、500→600 +0.001、600→700 +0.032 → **400 轮之后没有任何系统性上升，只有噪声**（0.40~0.44 之间摆动）。

**⚠️ 修正 450 轮试点时的判断**：当时据"300→400 增益 +0.0435、平滑曲线到 420 轮仍创新高"得出"未饱和"。750 轮这次显示，**单次训练里 300~400 段的那个 +0.04~+0.15 增幅本身就落在重训噪声内**——同设置两次独立训练在同一批 epoch 上的逐点差可以到 0.13~0.17（450-run 在 epoch 300 比 750-run 高 0.1727）。**"某段还在涨"必须用两次以上独立训练确认，单次曲线不能作数。**

**自评 val 曲线会一直涨，绝不可作为停止/饱和依据**：

| 预算 | 自评最优轮 | 自评末轮 | 对 93 张训练图的留出式自评（最后 50 轮均值） |
|---|---|---|---|
| 300 | 276 | 0.9003 | 0.8987 |
| 450 | 433 | 0.9259 | 0.9341 |
| 750 | 739 | **0.9536** | **0.9605** |

自评曲线在 750 轮里单调升到 0.95+（对 93 张训练图"留出式"自评甚至到 0.96），而独立集合上早在 400 轮就停止上升。→ **自评越高只是越会背训练集**，用它判饱和会一路训到最过拟合的检查点。

**结论（三条，可用于论文与后续实验设计）**：

1. **实用预算区间 ≈ 100~400 轮**：400 轮之后在独立集合上无系统收益（收益已被噪声淹没），而计算成本线性增长。主表预算建议取该区间（如 300），并**必须配选权**。
2. **选权收益稳定在 +0.055~+0.058**（三次独立训练分别 +0.0549 / +0.0575 / +0.0576）——**这是本项目里最稳的一条效应**，61val 方案因此站得住。
3. **必须报告噪声**：同预算逐点差 std ≈0.067~0.086、中位绝对差 0.036~0.127；据此，臂间差值要用选权/多种子/多轮平均来压噪，并在论文中声明量级。

## 八、当前状态

- [x] **P1 已执行**（2026-09-20）：61test 0.3938 / 61val 上界 0.4487（虚高 +0.0549）；交叉验证选权增益 **+0.038**（+0.013 ~ +0.062）；快照 std 0.1396
- [x] **饱和试点 450 轮**（2026-09-20）
- [x] **延长试点 750 轮**（2026-09-20）：**400 轮后无系统增益**；自评曲线不可判饱和；选权增益 +0.0576
- [ ] **P2（下一步）**：三臂同预算（建议 **300~400 轮**）**全量训练** + 61val 选权；若时间允许，对 s 家族补 2 个种子以量化并压低噪声
- 结论落定后：写回 `PROGRESS.md`（结果与结论 / 路线图）、`COMMANDS.md`（训练与评测命令）、`paper\PAPER_PLAN.md`（Methodology 3.5 评测协议 + Discussion 口径对比）

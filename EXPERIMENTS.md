# EXPERIMENTS.md — 实验总纲与新会话入口（2026-09-20 立）

> **本文件是新会话的第一入口**：先读这里，再按需读 `PROGRESS.md`（进度与结果）、`COMMANDS.md`（命令）、`List_of_Experiments.md`（待办清单）。
> 结论数字以 `experiments\` 与 `phase10_v3_main.json` 为准；本文件只讲"我们在跑什么、为什么、怎么区分"。

---

## 零、新会话接手须知

**一句话现状**：用 Qwen-Image-Edit 编辑真实巡检照片合成"违规状态"样本，训练气瓶检测器；当前已完成**评测口径的方法学验证（L3）**，**主线三臂对照（L1）与协议对照（L2）尚未开跑**，卡在 B 臂增广脚本上。

**立即可做（按顺序）**：

| # | 待办 | 说明 | 成本 |
|---|---|---|---|
| 1 | **写 `scripts\make_aug93_dataset.py`** | 93 张真实照片离线增广到 **1085 张**（几何+色彩，同步变框，不用上下翻转，只对总量不配正负比）→ `D:\gas_cylinders\aug93\` | 开发 ≈1 h |
| 2 | **跑 L1+L2**（一次训练两种口径） | 三臂 × 六权重 × **300 轮**、全量、`save_period=10` → 每权重 30 个快照 → 逐快照评 61 张 → 一条曲线读 61test 与 61val | **≈6 h GPU** |
| 3 | **规模曲线改口径** | **493 vs 1085 两点、均 300 轮**：1085 点复用 L1 的 C 臂；只需新增 493 点 | **≈3.3 h GPU** |
| 4 | 方法学补充 | 复核价值消融、空标图消融、多种子、工作点 PR、误差分析 | 见清单 |

**最容易搞错的三件事**：

1. **P2 不是第四个臂**。它是 L2：同一批训练产物换评测口径（61test 报末轮 ↔ 61val 报选权值），只回答"61val 这套评测方案有没有价值"。
2. **`gen493` 是 `gen1085` 的子集**（训练清单重叠 314 张）→ **两批不可合并计数**；三臂按独立图片数对齐 = 93 / 1085 / 1085。
3. **不要用自评 val（同源）选权，也不要用自评曲线判饱和**：实测自评在 750 轮里一路涨到 0.95+ 而独立集合 400 轮就走平；同源自评选出的 `best.pt` 在独立 test 上平均差 0.025。

**别踩的坑（详见 `PROGRESS.md` 五节）**：`patience=0` 关早停；`data.yaml` 与清单用绝对路径；独立 python 脚本要 `__main__` 守卫 + `workers=0`；训练/评测需 danger-full-access（沙箱下 `WinError 5`）；输出重名会生成 `xxx-2`（先删空壳或用 `normalize_runs.ps1`）；`logs\` 与 `runs\` 不入库，实验数字要另行落到 `experiments\`。

---

## 一、三条实验线（**不要混**）

| # | 实验线 | 回答什么问题 | 数据与模型 | 状态 |
|---|---|---|---|---|
| **L1 主线：三臂同源对照** | **"为什么要用编辑合成，而不是免费的传统增广"**——方法与工程结论 | 三臂同源（都以 real93 为唯一来源）：A 真实 **93** / B 真实+传统增广 **1085** / C 编辑合成 **1085**；六权重统一超参 | 待跑（脚本 + 训练） |
| **L2 方法学线：61val 协议验证（**即 P2**）** | **"61val 这套评测口径本身有没有价值"**——**协议**结论，不是数据结论 | 复用 L1 的同一批训练产物，只换**评测与选权口径** | 待跑（依赖 L1） |
| **L3 支撑线：预算与噪声实验** | "该训多少轮""噪声多大""读哪个检查点" | 单权重试点（300/450/750 轮） | ✅ 已完成 |

**L1 与 L2 的区别（关键）**：

- **L1 变的是"训练数据"**（A/B/C 三臂），测**数据来源的效应**；
- **L2 变的是"评测与选权口径"**（61test 报末轮 ↔ 61val 报选权值），**训练产物完全相同**，测**协议效应**；
- 因此 L2 **不是**第四个臂，也**不产生**新的数据结论；它是 L1 结果的"两种读法"对照。

## 二、统一协议（v3，2026-09-20 定稿）

| 项目 | 取值 | 说明 |
|---|---|---|
| **epochs** | **300**（今后所有重训统一） | L3 实测：独立集合 400 轮后无系统增益；自评曲线不可判饱和 |
| imgsz / batch / device | 640 / 16 / 0 | 三臂一致 |
| `patience` | **0**（关早停） | 固定预算 |
| `save_period` | **10** | 为 L2 的"一条曲线两种口径"提供快照 |
| `close_mosaic` | 10（**须声明占比**：300 轮 3.3%，100 轮 10%） | 比较时须同预算、同占比 |
| 训练侧 val | 指向 **train 自己** | 仅满足 ultralytics 要求；不得指向评测集 |
| seed | 0（默认） | 多种子实验另行指定 |

## 三、L1 主线设计

**同源原则**：三臂都以 real93（93 张）为唯一数据来源，只差"怎么扩"。

| 臂 | 训练数据（**全量，不划自评 val**） | 数据配置 |
|---|---|---|
| A 真实臂 | **93 张** | `D:\gas_cylinders\v3\data_real93.yaml` |
| B 真实+传统增广臂 | **1085 张**（93 张离线增广） | `D:\gas_cylinders\aug93\data_aug93.yaml`（脚本待写） |
| C 编辑合成臂 | **1085 张** | `scripts\splits_gen1085\data_gen1085_full_v3.yaml`（清单 `gen1085_full_train.txt`） |

增广规则（B 臂）：hflip / ±10° 旋转 / 缩放平移 / 亮度对比度 HSV；**不用上下翻转**（工地有重力方向）；几何变换同步变框；**只对齐总量（1085），不配正负比**（标注单位是框，一张图上可混合）。

**评估集**：61 张网络图（`D:\gas_cylinders\new_test_set\`，72 框）——与 93 张巡检照片及其全部编辑产物**无来源/场景重叠**，独立性由 `scripts\check_test_independence.py` 复验（test↔real93 max 0.730、test↔gen800 max 0.732、对照 real93 内部 0.834）。

**两种报数口径（L2 就是它们的对照）**：

| 口径 | 选权 | 报数 | 性质 |
|---|---|---|---|
| **61test 方案**（现行） | 不选权，报 `last.pt` | 61 张上的 mAP | **泛化读数**（该集合未参与任何决策） |
| **61val 方案**（候选） | 在 61 张上选最优 epoch | 同一 61 张上的 mAP | **选择指标/上界**（非泛化估计） |

**L1 待产出**：表 1（61test 口径，三臂 × 六权重）+ 表 2（61val 口径，同结构 + 选权增益）。

## 四、L2 实验设计（P2）——验证 61val 方案

**流程（各臂只训一次）**：① 按 L1 协议训练（300 轮、全量、`save_period=10`）→ 每权重 30 个快照；② 每个快照在 61 张上评一次 → 快照曲线；③ 同一条曲线读两种口径：61test = 末轮值，61val = 曲线最大值；④ 汇总选权增益、虚高幅度、选权 epoch 分布。

**判据（L3 先验）**：L3 三次独立训练实测选权增益 **+0.055~+0.058**；若 L2 在三臂全量设置下复现该量级 → 61val 方案成立（报数须声明为 selection metric）。

**L2 不做的**：不加数据、不改臂定义、**不用同源自评 val 选权**（那条路已单独否证）。

## 五、L3 支撑线结果（已完成）与耗时模型

| 试点 | 设置 | 训练耗时（实测） | 关键结论 |
|---|---|---|---|
| P1 | A 臂 + yolo26s，**300 轮** | **9.5 min**（1.87 s/轮） | 61test 0.3938 / 61val 上界 0.4487（虚高 +0.0549）；交叉验证选权增益 **+0.038**（+0.013~+0.062） |
| 450 轮 | 同上，450 轮 | **13.9 min**（1.81 s/轮） | 曾判"未饱和"（后被 750 轮推翻） |
| 750 轮 | 同上，750 轮 | **23.5 min**（1.85 s/轮） | **400 轮后无系统增益**；自评升到 0.9536 而独立集合走平；选权增益 +0.0576 |

原始数据（**已入库**）：`experiments\p1_61val_vs_61test\snapshot_curve.json`、`cross_split.json`、`experiments\saturation_pilot\pilot_450.json`、`pilot_750.json`。
相关脚本：`scripts\p1_run.ps1`、`p1_eval_snapshots.py`、`p1_cross_split.py`、`pilot_saturation*.ps1`、`pilot*_eval.py`。

### 耗时模型（实测校准，用于排期）

**每轮耗时随图片数近似线性**：

| 训练集 | 图片数 | yolov8s 每轮 | 依据 |
|---|---|---|---|
| real93 | 93 | **0.90 s** | 实测（300/450/750 三点一致） |
| gen493 train | 394 | 4.08 s | 实测 |
| gen1085 train | 868 | 8.71 s | 实测 |
| gen493 全量 | 493 | ≈5.1 s | 线性外推（93→868 两点拟合，偏差 <6%） |
| gen1085 全量 | 1085 | ≈11.2 s | 同上 |

**排期（300 轮 × 六权重）**：

| 批次 | 内容 | 估算 |
|---|---|---|
| L1+L2 | A（93）+ B（1085 增广）+ C（1085） | A ≈13 min、B ≈2.6 h、C ≈3 h → **≈6 h** |
| L3 补 | 规模曲线 493 点（1085 点复用 C 臂） | **≈3.3 h** |
| 合计 | 一个 GPU 窗口串行 | **≈9.3 h**（外加评测 ≈40 min） |

> ⚠️ 此前的两个估算都已修正：三臂重训原说"4~6 h"偏小约一倍；A 臂原说"1 h"偏大约 4 倍。

## 六、目录规范：什么在库里、什么不入库、临时文件放哪

| 目录 | 内容 | 入库？ | 说明 |
|---|---|---|---|
| `experiments\` | **实验原始数据与结论**：`p1_61val_vs_61test\`（快照曲线、交叉验证）、`saturation_pilot\`（450/750 轮试点、SHA256） | ✅ **入库** | 小 JSON（几 KB）。**新的实验结论请放这里**——`runs\` 与 `logs\` 都不入库，放那里等于丢 |
| 仓库根 `*.md` | 台账与总纲（`EXPERIMENTS` / `PROGRESS` / `COMMANDS` / `List_of_Experiments` / `PLAN_61VAL`） | ✅ | |
| `phase10_v3_main.json` | 权威结果汇总（旧口径：100 轮、394/868） | ✅ | L1 完成后会被新汇总取代 |
| `scripts\` | 全部代码（训练/评测/配置/工具） | ✅ | |
| `runs\` | 训练与评测产物目录 | ❌ 忽略 | 见下 |
| `logs\` | 训练/评测日志（`v3_<arm>_*.log` 等） | ❌ 忽略 | 体积大、可重跑 |
| `weights\` | COCO 预训练源权重 | ❌ 忽略 | 大二进制 |
| `_trash\` | 弃用归档区 | ❌ 忽略 | 只归档、不引用 |
| `paper\reference\` | 文献 PDF 库 | ❌ 忽略 | 版权/体积 |
| `D:\gas_cylinders\` | 数据区（真实照片、两批生成图、61 张 test、各 yaml） | 仓库外 | 不进 git |

**`runs\detect\` 里各文件是什么**（全部由 ultralytics 依训练命令生成，**都不入库**）：

| 文件/目录 | 是什么 | 处理建议 |
|---|---|---|
| `<run>\args.yaml` | 该次训练的**完整超参记录**（含 data 路径、epochs、patience、save_period）——"这个权重是怎么训出来的"的唯一凭据 | **保留**，排查口径必看 |
| `<run>\results.csv` | 每轮的训练/自评 val 指标与累计耗时（`time` 列即实测每轮耗时来源） | **保留**（耗时模型与自评曲线都靠它） |
| `<run>\weights\last.pt` | 协议报数用的权重 | **保留** |
| `<run>\weights\best.pt` | 同源自评 val 选出的权重（本协议不用；打标件用 `last.pt`） | 可删或归档 `_trash\runs\` |
| `<run>\*.png` / `labels.jpg` / `train_batch*.jpg` / `val_batch*.jpg` | 训练曲线图与批次预览 | 可删；论文用图另画 |
| `v3\<arm>_<tag>\` | **评测输出**（PR/F1 曲线、混淆矩阵、预测预览） | 保留（结论的可视化留档） |
| 顶层 `_p1_*.txt` / `_p1_*.yaml` | P1 交叉验证脚本生成的临时子集清单（把 61 张切成前后两半用的） | 可删（脚本 `p1_cross_split.py` 会重建） |
| 顶层 `*.json` | 曾经的实验结论位置（**已全部迁到 `experiments\`**） | 今后不要再往这里写结论 |

**结论放哪（硬规则）**：训练产物留在 `runs\`（不入库、可重跑）；**实验结论与原始数据写进 `experiments\`**（入库）；日志留 `logs\`（不入库，只作过程记录）。

## 七、相关文件

| 文件/目录 | 作用 |
|---|---|
| `EXPERIMENTS.md` | **本文件**：新会话入口 + 实验总纲 |
| `PROGRESS.md` | 唯一进度台账（结果、产物、下一步、坑） |
| `COMMANDS.md` | 命令表（训练/评测/打标/GUI/已知坑） |
| `List_of_Experiments.md` | 实验操作清单（可勾选） |
| `PLAN_61VAL.md` | 61test/61val 协议论证与 P1、两轮饱和试点的完整记录 |
| `phase10_v3_main.json` | 权威结果数字（L1 产出，旧口径） |
| `experiments\` | **实验原始数据（入库）**：P1 与饱和试点的 JSON |
| `scripts\` | 全部代码（训练/评测/配置/工具） |
| `_trash\` | 弃用归档区（不入库；含旧 run、旧脚本、旧文档） |
| `paper\` | 论文工作区（PAPER_PLAN / working_paper.tex / references.bib） |

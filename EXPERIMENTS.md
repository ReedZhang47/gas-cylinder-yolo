# EXPERIMENTS.md — 实验总纲（2026-09-20 立）

> 定位：**三条实验线的主文档**，回答"我们在跑什么实验、每条线为了回答什么、彼此怎么区分"。
> `List_of_Experiments.md` 是可勾选的操作清单（本文件是它的上位说明）；`PLAN_61VAL.md` 是协议论证与判别的专题记录。
> 进度与结果以 `PROGRESS.md` 为准，数字以 `phase10_v3_main.json` 等结果文件为准。

## 一、三条实验线（**不要混**）

| # | 实验线 | 回答什么问题 | 数据与模型 | 状态 |
|---|---|---|---|---|
| **L1 主线：三臂同源对照** | **"为什么要用编辑合成，而不是免费的传统增广"**——方法与工程结论 | 三臂同源（都以 real93 为唯一来源）：A 真实 **93** / B 真实+传统增广 **1085** / C 编辑合成 **1085**；六权重统一超参 | 待跑（预算与协议见下） |
| **L2 方法学线：61val 协议验证（**即 P2**）** | **"61val 这套评测方案本身有没有价值"**——**协议/方法学**结论，不是数据结论 | 用 L1 的同一批训练产物，只换**评测与选权口径**做对照 | 待跑（依赖 L1） |
| **L3 支撑线：预算与噪声实验** | "该训多少轮""噪声多大""读哪个检查点"——为 L1/L2 定参数 | 单权重试点 | ✅ 已完成（300/450/750 轮） |

**L1 与 L2 的区别（关键）**：

- **L1 变的是"训练数据"**（A/B/C 三臂），测的是**数据来源的效应**；
- **L2 变的是"评测与选权口径"**（61test 报末轮 ↔ 61val 报选权值），**训练产物完全相同**，测的是**协议效应**；
- 因此 L2 **不是**第四个臂，也**不产生**新的数据结论；它是 L1 结果的"两种读法"对照。

## 二、统一协议（v3，2026-09-20 定稿方向）

| 项目 | 取值 | 说明 |
|---|---|---|
| **epochs** | **300**（今后所有重训统一） | L3 实测：独立集合上 400 轮后无系统增益（300→400 的 +0.152 属重训噪声）；自评曲线不可用于判饱和（会一路涨到 0.95+） |
| imgsz / batch / device | 640 / 16 / 0 | 三臂一致 |
| `patience` | **0**（关早停） | 固定预算 |
| `close_mosaic` | 10（**须声明占比**：300 轮下 3.3%，100 轮下 10%） | 比较时必须同预算、同占比 |
| 训练侧 val | 指向 **train 自己** | 仅满足 ultralytics 要求；不得指向评测集（否则训练日志每轮偷看 benchmark） |
| seed | 0（默认） | 多种子实验另行指定 |

## 三、L1 主线实验设计

**同源原则**：三臂都以 real93（93 张）为唯一数据来源，只差"怎么扩"。

| 臂 | 训练数据（**全量，不划自评 val**） | 说明 |
|---|---|---|
| A 真实臂 | **93 张** | 原始真实数据基线 |
| B 真实+传统增广臂 | **1085 张**（93 张离线增广） | hflip / ±10° 旋转 / 缩放平移 / 亮度对比度 HSV；**不用上下翻转**（工地有重力方向）；几何变换同步变框；**只对齐总量，不配正负比**（标注单位是框） |
| C 编辑合成臂 | **1085 张**（`gen1085` 全量） | Qwen-Image-Edit 编辑合成的"违规状态"样本 |

**评估集**：61 张网络图（`new_test_set`，72 框，与源数据及全部生成图无来源/场景重叠，独立性由 `check_test_independence.py` 复验）。

**两种报数口径（L2 就是它们的对照）**：

| 口径 | 训练数据 | 选权 | 报数 |
|---|---|---|---|
| **61test 方案**（现行） | 各臂全量 | 不选权，报 `last.pt` | 61 张上的 mAP（该集合未参与任何决策 → 可作泛化读数） |
| **61val 方案**（候选） | 各臂全量 | 在 61 张上选最优 epoch | 同一 61 张上的 mAP（**选择指标/上界**，不是泛化估计） |

**待生成的主表**（L1 产出，两种口径各一份）：
- 表 1：61test 口径 —— 三臂 × 六权重 × 61 张（`last.pt`），与规模曲线合并成论文主结果；
- 表 2：61val 口径 —— 同三臂 × 六权重，报选权值 + 选权增益 + 虚高说明。

**去重后的数据资产**（⚠️ `gen493` 是 `gen1085` 批次的**子集**，两者 train 清单重叠 314 张 → **不可合并计数**）：

| 资产 | 独立图片数 | 用途 |
|---|---|---|
| real93 | 93 | A 臂 / B 臂增广源 / C 臂编辑源 |
| gen1085 全量 | 1085 | C 臂（清单 `scripts\splits_gen1085\gen1085_full_train.txt`，yaml `data_gen1085_full_v3.yaml`） |
| gen493 子集（394/493） | ⊆1085 | 仅用于**规模曲线**（394 vs 868 两个点），不进三臂主表 |
| new_test_set | 61（72 框） | 评估集（双重角色见上表） |

## 四、L2 实验设计（P2）——验证 61val 方案

**目的**：判定"在独立集合上选权"的评测口径是否值得作为论文主口径。**训练产物复用 L1**，因此成本主要是训练一次而非两次。

**流程（全量训练，各臂只训一次）**：

1. 按 L1 协议训三臂 × 六权重（**300 轮、全量、`save_period=10`**）→ 每臂每权重得到 30 个快照；
2. 每个快照在 61 张上评一次 → 得到"快照曲线"；
3. 由同一条曲线读出两种口径：
   - **61test**：末轮值（epoch300）；
   - **61val**：曲线最大值（含其 epoch）；
4. 汇总：选权增益（逐臂逐权重）、虚高幅度、选权 epoch 分布。

**判据（L3 已给出先验，供 L2 对照）**：L3 实测选权增益稳定在 **+0.055~+0.058**（三次独立训练），且自评曲线在 400 轮后仍在涨而独立集合走平。若 L2 在三臂全量设置下复现该量级 → 61val 方案成立，可作主口径（但报数须声明为 selection metric）。

**L2 不做的事（避免与 L1 混淆）**：
- 不新增训练数据、不改臂定义、不重定义 B 臂；
- 不用自评 val（同源）选权 —— 那条路已被单独否证（在同源自评 val 上选权，独立 test 上平均差 0.025）。

## 五、L3 支撑线（已完成）与实测耗时

| 试点 | 设置 | 训练耗时（实测） | 关键结论 |
|---|---|---|---|
| P1 | A 臂 + yolo26s，**300 轮** | **9.5 min**（1.87 s/轮） | 61test 0.3938 / 61val 上界 0.4487（虚高 +0.0549）；交叉验证选权增益 **+0.038**（+0.013~+0.062） |
| 450 轮 | 同上，450 轮 | **13.9 min**（1.81 s/轮） | 当时判"未饱和"（后被 750 轮推翻） |
| 750 轮 | 同上，750 轮 | **23.5 min**（1.85 s/轮） | **400 轮后无系统增益**；自评升到 0.9536 而独立集合走平；选权增益 +0.0576 |

### 耗时模型（实测校准，用于排期）

**实测每轮耗时随图片数近似线性**：

| 训练集 | 图片数 | yolov8s 每轮 | 依据 |
|---|---|---|---|
| real93 | 93 | **0.90 s** | 实测（300/450/750 轮三点一致） |
| gen493 train | 394 | 4.08 s | 实测（现役 run 的 results.csv） |
| gen1085 train | 868 | 8.71 s | 实测（现役 run 的 results.csv） |
| **C 臂（全量）** | **1085** | **≈11.2 s** | 线性外推（93→868 两点拟合，偏差 <6%） |

**P2/L1 三臂 × 六权重 × 300 轮的排期（按上述模型外推）**：

| 臂 | s 级 ×3 | m 级 ×3 | 小计 |
|---|---|---|---|
| A（93 张） | ≈4.5 min | ≈8.5 min | **≈13 min** |
| B（1085 张增广） | ≈56 min | ≈100 min | **≈2.6 h** |
| C（1085 张，m 级系数按 gen1085 实测 2.2×） | ≈56 min | ≈120 min | **≈3 h** |
| **合计** | | | **≈5.7 h**（外加评测 ≈40 min） |

> ⚠️ **修正**：此前"三臂重训 ≈4~6 h"的估算**偏小约一倍**（当时误按 100 轮/1000 张的每轮 20 s 外推）；按实测应为 **≈6 h**。而 P1 300 轮的 9.5 min 也说明**小数据臂的成本远低于早先估计（曾写 1 h）**。

## 六、相关文件

| 文件 | 作用 |
|---|---|
| `PROGRESS.md` | 唯一进度台账（结果、产物、下一步、坑） |
| `List_of_Experiments.md` | 实验操作清单（可勾选） |
| `PLAN_61VAL.md` | 61test/61val 协议论证、P1 与两轮饱和试点的完整记录 |
| `COMMANDS.md` | 命令表（训练/评测/打标/GUI/坑） |
| `phase10_v3_main.json` | 权威结果数字（L1 产出） |
